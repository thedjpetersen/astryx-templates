# Re-review pass 1 (2026-07-03) — 12 previously-broken templates after fix batches 1-2

Same pipeline as the review cycles. These templates previously carried the worst high-severity findings; this pass verifies fixes held and catches residuals/regressions.
**Residual totals: 1 high / 15 medium / 17 low across 12 re-reviewed templates (previously these 12 carried 20+ highs).**


## okr-tree (7 confirmed residual)

- **HIGH** [both] OKR table, Objective/key result column: Primary titles and subtitles are aggressively truncated (e.g. 'Make onboarding sel…', 'Cut median time-to…', 'now 4.2 min · updated …') despite ~150px of empty whitespace before the Owner column, suggesting a broken max-width on the text container; nearly every row's title is unreadable.
- **MEDIUM** [both] OKR table, Owner column avatars: Avatar initials have very low contrast: near-white/gray text on a light-gray circle in light mode, dark-gray text on a slightly darker gray circle in dark mode; initials are barely legible.
- **MEDIUM** [dark] Template header, 'At risk only' toggle: In dark mode the unchecked toggle track is nearly the same shade as the dark header background, making the switch almost invisible/appear broken.
- **LOW** [both] OKR table, Progress column: Progress percentage (e.g. '69%') is pushed far right of its bar, visually grouping with the Confidence badge instead of the progress bar it describes.
- **LOW** [both] Summary cards, Company card: Subtitle wraps so the separator dot dangles at the start of the second line ('· week 12 of 13'), an awkward typographic orphan.
- **LOW** [both] Summary cards section: Three cards in a 2-column grid leave a conspicuous empty hole below the Aggregate progress card, with the Confidence card orphaned on the second row; the section looks structurally unbalanced.
- **LOW** [both] Template header, Expand all / Collapse all: 'Expand all' and 'Collapse all' actions are styled as plain body text with no button container, underline, or accent color, giving no clickable affordance (identical styling to the non-interactive 'At risk only' label).

> Pipeline notes: Pipeline healthy: shoot.mjs succeeded on first run (2 segments per scheme, 4 files in /tmp/astryx-uiux/shots), no blank-panel flake in any capture. Gemini critique succeeded on first attempt with 8 findings; 7 verified against screenshots, 1 dropped (claimed left-aligned Score column is not visually verifiable since all values are equal width). No template-preview defects found beyond gemini's list.

## portfolio-holdings (2 confirmed residual)

- **MEDIUM** [both] Allocation legend vs Holdings table category pills: Asset-class colors are inconsistent between components: the Allocation donut legend maps Cash=purple, Fixed Income=orange, Intl Equity=green, but the Holdings table pills render Cash=amber, Fixed Income=green, Intl Equity=purple (only US Equity=blue matches). The same asset class is color-coded differently in the chart and the table on one screen.
- **MEDIUM** [dark] Positive gain/loss text (header summary stats, Holdings table gain/loss column and total row, Watchlist deltas): Green gain text uses the identical light-mode color #0c8625/#0d8626 in dark mode (pixel-sampled), yielding ~3.5:1 contrast on the #1f1f22 dark card background (below WCAG 4.5:1 for small text), while red loss text correctly adapts from #e3193b (light) to a brighter #f5394f (dark). The positive-status green token appears hardcoded / missing its dark-scheme variant, making gains noticeably dimmer than losses in dark mode.

> Pipeline notes: Pipeline healthy: shoot.mjs succeeded on first run with 2 scroll segments per scheme, no blank-panel flake in any capture. Gemini critique succeeded on first call and returned 8 findings; 6 were rejected on verification (donut slice proportions actually match the legend percentages; Cash and Fixed Income pill text is legible in both schemes per zoomed crops; dark secondary text is muted but readable; the 'sticky header bleed' finding referenced gallery chrome plus a scroll-capture shear; sparkline bottom-anchored bars are design intent). Finding 7 was kept but corrected: only green is hardcoded, red adapts properly. Colors verified by pixel sampling via ffmpeg.

## payout-statements (2 confirmed residual)

- **MEDIUM** [both] Payouts header / filter row (scrolled state): When the panel is scrolled (2of2 segments), the filter pills and Export CSV button stay pinned while the 'Payouts' title scrolls beneath them and is clipped mid-glyph, with no background, border, or shadow on the pinned row to separate it from the sliced text — the header looks broken in the scrolled state.
- **LOW** [light] Gross-to-net waterfall in detail pane: In light mode the empty background tracks for the Platform fee, Refunds, and Adjustments bars are an extremely faint gray that is nearly invisible against the white detail pane, so the small red deduction segments appear to float without context (dark mode tracks are clearly visible).

> Pipeline notes: Pipeline healthy: shoot.mjs succeeded on first run with 2 scroll segments per scheme (4 files), no blank-panel flake in any capture. Gemini critique succeeded on first call and returned 8 findings; 5 were dropped as factually wrong or intentional design (missing selected-row state, header misalignment, inverted Default badge, right-anchored waterfall segments, generic muted-text contrast), and two scroll-state findings were merged into one confirmed finding.

## wallet-card-fan (3 confirmed residual)

- **MEDIUM** [dark] card fan + sidebar CARDS swatches: Neutral token-gradient cards (Sable Reserve, Everyday Checking) render near-black in light mode but invert to light/medium gray in dark mode, leaving their white titles and gray eyebrow labels (e.g. 'HARBORLINE · DEBIT') with clearly reduced contrast; sidebar card swatches flip black-to-gray the same way, so the 'brand' colors are inconsistent between schemes.
- **MEDIUM** [both] overall layout when scrolled (ledger view): Once scrolled past the card fan, the entire right half of the preview panel is an empty void: the Wallet summary / CARDS column is not sticky and the ledger list stays at fixed narrow width instead of expanding, producing an unbalanced page in segments 2of3 and 3of3.
- **LOW** [both] helper text below card fan: Redundant duplicated instruction copy: 'Tap a card to bring it forward, or press ↑ / ↓' appears directly above the shortcut pills and 'Tap a card to bring it forward — tap again to flip it over.' directly below them, repeating the same sentence twice with different endings.

> Pipeline notes: First shoot.mjs run failed with a 30s page.screenshot timeout; retry succeeded and all 6 captures (light/dark x 3 segments) rendered non-blank, so no blank-panel flake recaptures were needed. Gemini returned 8 findings but 5 were dropped on pixel verification: its top claims of 'nearly invisible' dark-mode ledger amounts, subtitles, helper text, and sidebar metadata are false (enlarged crops show white amounts and legible gray secondary text), its 'disappearing container borders' claim is not visible in crops, and its 'clipped scroll content' is just normal viewport scroll position, not a template defect. The dark-mode gray-card contrast finding was added by direct inspection.

## venue-seat-picker (5 confirmed residual)

- **MEDIUM** [both] seat map minimap (bottom-left of map canvas): Minimap content and viewport indicator are near-invisible: pale blue-gray shapes on a pale card in light mode and faint dark outlines on near-black in dark mode; no discernible viewport rectangle, so the widget fails its purpose of showing which part of the map is in view.
- **MEDIUM** [dark] seat map grid (dark mode): Sold seats render as very dark gray dots that nearly merge with the near-black map background, reading as empty gaps rather than a disabled state; the Sold legend swatch has the same low-contrast problem.
- **MEDIUM** [both] seat map section labels (MEZZANINE / BALCONY): Section labels are drawn directly on top of seat dots (e.g. MEZZANINE overlaps a seat dot mid-word in both schemes, BALCONY sits over dense dots); the text halo keeps them mostly readable but the collision looks unpolished.
- **LOW** [dark] price-tier color ramp (seat dots and legend, dark mode): The four-tier green price ramp compresses in dark mode: the Value ($48) swatch and seats are a very dark desaturated green hard to distinguish from Standard and from the Sold gray, weakening the tier encoding.
- **LOW** [dark] Pricing & legend floating panel (dark mode): Panel lacks visible elevation/separation in dark mode: its background is nearly the same value as the map canvas and the shadow vanishes, so the edge is only discernible where seat dots stop; a subtle 1px light border would fix it.

> Pipeline notes: Pipeline healthy: shoot.mjs succeeded on first run (2 scroll segments per scheme, none blank), gemini critique succeeded on first call. 4 of gemini's 8 findings were dropped after pixel verification: the 'scroll clipping/z-index leak' is a normal mid-scroll capture state, and the companion-icon cramping, Find-button icon spacing, and legend bottom-padding claims were contradicted by zoomed crops. One aside: in dark.2of2 the gallery chrome rendered light-themed while the preview stayed dark — chrome-only, excluded per instructions, likely a harness theming race.

## mail-compose (4 confirmed residual)

- **MEDIUM** [both] compose window form fields: Inconsistent left alignment: the error banner, subject input, body textarea, and Attachments header all start flush at the card's left padding (aligned with the 'To' label), but the To recipient field is indented ~100px to the right of them, creating a jagged left rail of field edges.
- **LOW** [both] compose window To row: 'Cc' and 'Bcc' render as bare unstyled text floating outside the right edge of the To field border, with no button affordance or bounding structure; they look like orphaned labels rather than clickable controls.
- **LOW** [dark] mail app backdrop behind compose window: In dark mode the skeleton placeholder rows (avatar circles and text bars) of the mail list backdrop are barely distinguishable from the background, whereas in light mode they are clearly visible; the dark backdrop reads as an empty/broken panel.
- **LOW** [dark] compose window invalid-recipient chip and error banner: In dark mode the invalid 'jordan.h@lumen' chip and the validation banner use muted pink text on dark maroon, noticeably lower contrast than their light-mode counterparts (still legible, but weaker than the other chips).

> Pipeline notes: Pipeline healthy: shoot.mjs succeeded on first run with non-blank previews in both schemes (2 scroll segments each, no blank-panel flake, no recapture needed). Gemini call succeeded on first attempt and returned 8 findings; 3 were factually wrong on zoom inspection (claimed dark-on-dark chip text, invisible To border, and indistinguishable progress bar — all disproven with 4x crops), 2 were not visible/native-browser nitpicks (radii clash, textarea resize handle), and 3 were confirmed. No additional defects found beyond gemini's confirmed set; a suspected Send split-button state mismatch was disproven at zoom.

## command-palette-launcher (3 confirmed residual)

- **MEDIUM** [both] palette results list (bottom, above footer): Last visible command row "/model" is chopped mid-glyph at the bottom of the scrollable results list, with its shortcut-key badges reduced to half-clipped slivers; there is no bottom padding or fade mask before the footer, so the list edge looks broken in every capture (light/dark, both segments).
- **MEDIUM** [both] palette footer (right-side hint text): Right-aligned footer hint reads ": workspace · / commands · ? help" with a floating leading colon after a gap, and the key characters (:, /, ?) are rendered as plain inline text while the left-side keys (up/down, enter, Esc) get styled badge chips — reads like a typo/missing badge.
- **LOW** [both] keyboard hint chips (palette input vs mock workspace header): Cmd-K hint chips use two different shapes in the same template: rounded-square chips in the palette search input vs fully circular chips in the backdrop workspace's header search, an inconsistent treatment for the same component.

> Pipeline notes: Pipeline healthy: shoot.mjs succeeded first try (2 segments per scheme, no blank-panel flake), gemini call succeeded first try. Gemini returned 8 findings; 5 were rejected after pixel-level crop verification: (a) dark-mode shortcut badges are light-gray glyphs on mid-gray pills, readable, not hardcoded dark; (b/c) low-contrast backdrop card text and header search in dark mode is the intentional dimmed-workspace effect, equally dim in light mode, so no hardcoded-color inconsistency; (d) the sticky footer does have a visible top divider line in both schemes; (e) active-row secondary text contrast in dark mode is acceptable. Also verified the fuzzy-match highlight boxes align correctly with matched characters (Dep / De+p / dep), so no highlight-offset bug.

## calendar-week-agenda (2 confirmed residual)

- **MEDIUM** [both] time grid — current-time (now) line: The red now-line renders on top of event blocks across all seven day columns at full opacity, striking through the '10 AM – 11 AM' time text of the Mon 'Portfolio review' and Tue 'Roadmap pre-read' events; events should either sit above the line outside today's column or the line should be dimmed/limited to today.
- **LOW** [both] all-day row and colored event blocks: White text on the orange all-day chip ('v2.1 release can…') and the semi-transparent white time subtext on bright green blocks (e.g. 'Farmers market') are low contrast (below WCAG AA); event colors are identical in both schemes so the issue appears in light and dark alike.

> Pipeline notes: Pipeline healthy: shoot.mjs produced 2 scroll segments per scheme (4 files, not 3) on the first run with no blank panels; gemini critique succeeded on the first call. 6 of gemini's 8 findings were dropped after pixel-level verification: clipped filter pills and 'missing sticky day headers' in the 2of2 segments are page-scroll capture artifacts of the gallery toolbar, not template bugs; claimed column bleed of overlap cascades (Tue 'Hiri…', checked Thu 'API pagi…' too) is not present in zoomed crops; event block heights use uniform ~1-3px insets around hour lines (no inconsistency); the now-line dot sits at the left edge of today's (Wed) column, which is standard; dark-mode hour axis labels are clearly legible in a zoomed crop. Overall the template is well-finished; only the now-line z-order/text strike-through and event text contrast survived.

## calendar-month-grid (2 confirmed residual)

- **MEDIUM** [dark] calendar grid (out-of-month cells): Dark mode drops the out-of-month cell background tint that light mode uses (pixel patches of out-of-month vs in-month cells are identical near-black), so adjacent-month days are distinguished only by dim gray date numbers (28/29/30 top-left, 1 bottom-right) that sit at low contrast on the near-black background — a light-vs-dark inconsistency.
- **LOW** [both] calendar grid (selected day cell, July 8): The 2px selection border on the active July 8 cell is drawn inside the cell and consumes the internal inset: event chips ('v2.1 release ca…', 'Design crit — …') sit flush against the left/right border with zero gap, and the circled '8' crowds the top edge, while unselected cells (e.g. July 9) keep a visible gap between chips and gridlines.

> Pipeline notes: Capture succeeded first try (2 scroll segments per scheme, none blank; no harness flake). Gemini returned 8 findings; 6 were dropped after pixel-level verification: (1) top-right nav 'Today' button is correctly light-styled in light mode, not dark-hardcoded; (2) light-mode 'Work' tag is tinted light-blue with dark-blue text, consistent with Deadlines/Team tags, not solid-fill; (5) dark 'Team' tag is light-purple-on-dark-purple and clearly readable at 4x zoom; (7) flight chips use a real → arrow glyph, not ASCII '->'; (8) agenda card vertical rhythm looks consistent at zoom; (6) the agenda 'Today' badge black-in-light/white-in-dark inversion matches the app's primary-token pill styling elsewhere and appears intentional, not a hardcoded color. Screenshots at /tmp/astryx-uiux/shots/, verification crops at /tmp/astryx-uiux/shots/crops/.

## transactions-ledger (1 confirmed residual)

- **LOW** [both] transaction table, Cleared column: Pending-row clock icons are indented ~13px right of the posted-row checkboxes and the section-header icons in the same Cleared column, breaking the column's vertical alignment axis (visible in light.1of2 and dark.1of2, rows Jul 2 - Jul 1 vs posted rows below).

> Pipeline notes: Capture succeeded first try; all 4 screenshots (2 light + 2 dark segments) rendered with content, no blank-panel flake. Gemini returned 8 findings but 7 were hallucinated or contradicted by close-up crops (e.g. it claimed dark-mode checkboxes were black cutouts when they are white with dark checks, and claimed the dark progress bar had a black fill when it is white-on-dark). Only the Cleared-column icon misalignment survived verification, and only partially (header icons actually align with checkboxes; the pending clock icons are the outliers). Template is otherwise in strong shape in both schemes with no light-vs-dark hardcoded-color inconsistencies.

## milestone-burnup (1 confirmed residual)

- **LOW** [both] KPI stat row — Forecast date card: Delta text "2 wks past target range Aug 10 – Aug 17" wraps with an orphan: the date range breaks between "Aug" and "17", leaving "17" alone on the second line. The date range should be non-breaking. Present in both schemes (light.1of2 and dark.1of2).

> Pipeline notes: Pipeline healthy: shoot.mjs succeeded on first run with non-blank previews in both schemes (2 scroll segments each, not 3). Gemini returned 8 findings but ALL failed pixel verification against 3x-zoomed crops: (1,3,4) the dark-mode strings it called "invisible dark text" (expanded-row Owner/Target/Remaining metadata, table helper text, callout footer) all render in legible light gray; (2) the red KPI delta text is the identical red across all four cards — no inconsistency, and it reads legibly on the dark card; (5) progress bars have normal spacing below the % text, not crashing the row border; (6) nested Done/In-progress badges are properly dark-themed (dark green/blue fills), not hardcoded light; (7) header At-risk pill padding matches the table pills; (8) the refresh icon is vertically centered with the segmented control. The single reported finding is one I identified during verification (gemini partially touched the same text in its finding 2 but for the wrong reason). Dark-mode chart forecast band and plot area verified consistent with light mode.

## table-inline-expansion (1 confirmed residual)

- **LOW** [both] table body, Endpoint column: Variable-width HTTP method badges (GET/POST/DELETE) push the endpoint paths to different horizontal start positions, giving the path column a ragged left edge; most visible on the wider DELETE /v1/tokens/:id row.

> Pipeline notes: Capture succeeded on first try; all 4 screenshots (light/dark x 2 segments) rendered non-blank. Gemini returned 8 findings but 7 did not survive pixel-level verification: dark-mode 'Related endpoints' description text is themed light gray (readable, not hardcoded dark); spacing before the next badge pair exists and is larger than intra-pair spacing; status badges are left-aligned flush with the header with ~45px clearance to the Owner column; expanded-region vertical padding is balanced; last row is not cramped; header actions are vertically centered. Only the jagged path alignment finding was visually confirmed. No additional defects found beyond gemini's list.
