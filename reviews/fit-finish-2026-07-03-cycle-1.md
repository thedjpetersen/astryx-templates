# Fit-and-finish review — cycle 1 (2026-07-03)

Pipeline: vite demo server → Playwright headless Chromium (1440x900, light+dark, scrolling segments of `.preview-wrap`) → Gemini designer critique → Claude agent pixel-crop verification. Only visually confirmed findings are listed.
**Totals: 4 high / 20 medium / 18 low across 12 templates.**


## webhook-delivery-debugger (5 confirmed)

- **HIGH** [both] delivery feed filter tabs (middle column): Segmented status-filter control is broken: every tab label wraps to two lines (e.g. 'All' over '(30)', '2xx' over '(21)'), and the active-tab pill only encloses the top line, so '(30)' visibly spills out below the pill background. Seen in light and dark 1of3.
- **MEDIUM** [both] delivery feed status badges (middle column): HTTP 401 and HTTP 422 badges use white text on an amber background, giving very poor contrast; inconsistent with the endpoint health badges (92%/86%/80%) which correctly use dark text on the same yellow. Seen in light/dark 1of3 and 2of3.
- **MEDIUM** [light] JSON payload detail pane (right column): The JSON tree pane keeps a hardcoded dark-navy background and dark-theme syntax colors in light mode; it is the only surface in the template that does not switch with the theme, suggesting hardcoded colors rather than tokens. Seen in light 1of3/2of3.
- **MEDIUM** [both] overall panel layout / scroll behavior: The delivery list renders all 30 rows with no internal scroll, stretching the template to ~3 screens; below the first screen the endpoint rail and detail pane are empty (detail pane is not sticky), leaving large blank columns in scroll segments 2of3 and 3of3.
- **LOW** [both] delivery feed and endpoint rail meta text: Secondary meta lines truncate mid-word on almost every row ('1 att…', '5 atte…', '3 event …'); the layout reserves badge space such that the attempt count nearly always clips, which reads as unpolished. Truncation is space-constrained (badges to the right), not gratuitous.

> Pipeline notes: shoot.mjs timed out on first run (page.goto networkidle 30s) but succeeded on retry; all 6 screenshots captured. Gemini call succeeded on first try and returned 8 findings. Verified each with pixel crops (sips): dropped gemini's claims of bottom-row clipping (row evt_6999 is fully visible), monochrome JSON syntax highlighting (keys cyan, strings green, numbers blue), column-header misalignment (~2px, intentional hierarchy), and low-contrast 'enqueues attempt 2' helper text (normal muted style). Added one finding gemini missed (no internal list scroll / non-sticky detail pane). Dark mode columns are correctly dark; no other hardcoded-color issues seen besides the JSON pane.

## cert-key-inventory (3 confirmed)

- **HIGH** [both] main table, Expires column: Every expiry status pill (e.g. 'Expired 101d ago', '6d left', '593d left') overflows the table and is clipped flat at the right edge where the detail drawer divider sits — the pill's right rounded cap and padding are cut off on every row, in both scroll segments.
- **MEDIUM** [both] main table, Expires column: Because the pills bleed to the container edge, the Expires column has zero right padding while other columns have generous padding; pills are right-flush but the dates beneath them and the 'Expires' header are left-aligned, making the column read as disjointed and shoved right.
- **LOW** [both] detail drawer, Chain section: The 'Intermediate' hierarchy label on the second chain row runs nearly to the drawer's right edge with visibly less right margin than other drawer content (copy icon, ALB pill, Rotate button), looking cramped; in dark mode the Root/Intermediate/Leaf labels are muted gray with low-ish (though still legible) contrast.

> Pipeline notes: First shoot.mjs run timed out on page.goto networkidle; retry succeeded (4 shots: light/dark x 2 segments). Gemini call succeeded on first try but 5 of its 8 findings were disproved under pixel-level crops: yellow '6d left' badges use dark text (not white-on-yellow), the drawer header has adequate top padding, filter-tag row gaps look even, 'ALB us-east-1' aligns flush with the Rotate button edge, and the selected row has a clear dark left accent bar. Gemini's header-alignment and row-padding findings were kept but merged since they share the pill-overflow root cause. Dark mode otherwise looks correctly tokenized (Rotate button inverts, backgrounds/text all swap).

## theme-token-studio (6 confirmed)

- **HIGH** [both] preview pane headers (Light forced / Dark forced): Pane header labels use the host app's theme colors instead of the pane's forced scheme: in light mode the 'Dark forced' pane header renders 'Dark' and its badge in dark grey on a near-black header (nearly invisible), and in dark mode the 'Light forced' pane header renders light grey text on a white header. Verified in light.1of2 and dark.1of2.
- **HIGH** [both] deals table in both preview panes: The '+ New deal' button overlaps the last table row ('Citadel expansion'), occluding its stage badge (only half the pill is visible behind the button) and clipping the row text; row is also cut off by the card bottom. Visible in both panes in light.2of2 and dark.2of2.
- **MEDIUM** [both] deals table in both preview panes: Stage badges collide with the Amount column: the 'Won' pill overlaps '$12,400' and the 'In review' pill overlaps '$8,150' (the $ digit sits under the badge border) in both forced panes — the table columns are too narrow. Verified in light.2of2 and dark.2of2 crops.
- **MEDIUM** [both] preview pane headers: Text next to the 'forced' badge is truncated to 'light-da…' / 'light-dar…' in both pane headers, leaving a broken-looking fragment with no readable meaning. Visible in light.1of2 and dark.1of2.
- **LOW** [both] token editor (--brand-primary ramp step): Ramp step labels '100', '400', '900' overlap the top edges of the swatch squares beneath them instead of sitting on their own line, so the numbers collide with the swatches. Verified in light.1of2 crop.
- **LOW** [both] preview column header: The 'Preview · Lumen CRM' heading wraps mid-name ('Preview · Lumen' / 'CRM') while its subtitle sits beside it, making the header block look cramped. Visible in light.1of2 and dark.1of2.

> Pipeline notes: First shoot.mjs run timed out on networkidle; retry succeeded (4 screenshots, 2 segments per scheme). Gemini call succeeded on first try. Two gemini findings were dropped after visual verification: the Export card is correctly dark-themed in dark mode (not a hardcoded white background), and the low-contrast Undo/Reset-all buttons are almost certainly intentional disabled states since 0 of 15 tokens were edited. Gemini's top finding cited card body text ('Monthly recurring revenue') as illegible, but the body text is legible; the real theme bleed is in the forced pane header rows, which is what was reported. The badge/amount overlap finding was added from my own inspection (gemini missed it). Verified with sips crops at /tmp/astryx-uiux/crop_*.png.

## saas-landing-page (1 confirmed)

- **MEDIUM** [both] feature sections (Sync Engine / Audit Log / Roles & API eyebrow labels): Green eyebrow category labels render as full-column-width bars instead of content-hugging pills; the tinted background stretches across the entire text column in both light and dark mode (segments 2of3 and 3of3), inconsistent with the compact 'Features' and 'At a glance' pills elsewhere on the page — looks like a missing width:fit-content/inline display.

> Pipeline notes: Capture: first shoot.mjs run timed out (page.goto networkidle 30s); retry succeeded and produced all 6 screenshots. Gemini critique ran successfully and returned 8 findings, but 7 were disproved on zoomed crop verification: (1) audit-log terminal text is slate-on-white and legible, (2) feature checkmarks are green-on-mint, not white/invisible, (4) light-mode logo ticker has solid dark-slate text contrast, (5) CLI-first bento terminal has proper inset padding on all sides, (6) hero email input and button heights align within ~1px, (7) SSO/SCIM/Residency pills are white on medium green with adequate contrast, (8) icon-to-eyebrow spacing is normal. Only finding 3 (full-width eyebrow labels) survived. Also note: the shoot script captures at most 3 scroll segments, so the pricing teaser, FAQ, and footer described for this template were not covered by the screenshots and remain unreviewed. Crops used for verification are in /tmp/astryx-uiux/shots/crops/.

## multiplayer-whiteboard-replay (6 confirmed)

- **MEDIUM** [light] event log timestamp badges: Beat timestamp pills (t 0, t 2, t 4...) render as near-black pills with white text in light mode, visually identical to dark mode — suggests hardcoded colors; they look heavy next to the correctly-theming 0.5x/1x/2x and SPACE/J/K pills.
- **MEDIUM** [light] canvas frame label: The frame title 'Went well' at the top-left of the frame is rendered in very light gray on the off-white canvas and is barely legible in light mode.
- **MEDIUM** [both] footer playback bar: At t=0 the scrubber thumb sits flush against (and slightly overlaps) the 't 0 / 120' counter label — there is no gap between the counter and the start of the slider track, in both schemes.
- **LOW** [dark] header collaborator avatars: Avatar initials (Y, AF, LO, MC, TN) are muted gray on a dark avatar fill in dark mode — legible but borderline contrast for such small text; gemini's claim that they are completely unreadable was exaggerated.
- **LOW** [both] canvas minimap: The minimap sticky-note marker does not reflect the object's themed appearance: it renders as a solid black block in light mode and a solid white block in dark mode, the opposite of the actual sticky's color in each scheme.
- **LOW** [both] event log panel: The 'Event log / 21 beats' header is not sticky; when the page scrolls, list rows slice abruptly against the top divider of the preview panel (visible in both 2of2 segments).

> Pipeline notes: Capture: first shoot.mjs run timed out on page.goto networkidle; retry succeeded (4 shots: light/dark x 2 segments). Gemini call succeeded on first try and returned 8 findings. On visual verification with zoomed crops, 3 gemini findings were dropped as not visible (inverted scrubber track colors in light mode, premature event-log text truncation, heavy dark minimap container in light mode), 1 was downgraded (dark-mode avatar initials are low-contrast, not unreadable), and 1 defect gemini missed was added (scrubber thumb overlapping the t 0 / 120 counter).

## data-catalog-lineage (3 confirmed)

- **MEDIUM** [both] detail pane - columns table, Null rate column (payment_fee row): The 1.9% null-rate fill renders as a tiny detached dark tick at the left of the track, slightly taller than the track and visually separated from it, instead of a fill segment inside the track; it reads as a stray rendering artifact in both light and dark (segment 1of2 and 2of2).
- **MEDIUM** [both] detail pane - columns table, Type column: Data types are truncated prematurely (e.g. 'numeric(12,…' for gross_amount, payment_fee, net_amount) even though roughly 100px of empty space remains between the Type and Null rate columns; the full 'numeric(12,2)' would easily fit.
- **LOW** [both] list pane - Tags rail under the dataset search input: Tag filter chips ('core (6)', 'finance (5)', etc.) wrap with unusually large (~30-40px) horizontal gaps, making the rail look scattered and ragged across its four wrapped rows rather than a tight chip group.

> Pipeline notes: Pipeline: first shoot.mjs run timed out on networkidle; retry succeeded and produced 2 scroll segments per scheme (not 3). Gemini call succeeded on first try and returned 8 findings, but close-crop verification (sips crops read at high zoom) refuted 5 of them: dark-mode selected-row contrast is fine (white text on dark gray), Order/Gross Revenue glossary tags do adapt to dark mode, right-edge padding is consistent (quality pill, sort button, and table right edges all align), the '+' add-tag button and the '97' quality text are both vertically centered, and gemini's claim that the 1.9% row 'drops the track' was inaccurate (track is present; the fill tick is what's broken). No additional defects found beyond gemini's confirmed ones; tab badges, source pills, and status dots all theme correctly in dark mode.

## sudoku-logic-coach (3 confirmed)

- **MEDIUM** [both] sudoku board, selected cell (R5C5): Pencil-mark digit inside the selected cell is very low contrast against the selection fill (grey-on-grey, roughly 2:1); readable but faint in light mode and nearly vanishing in dark mode, while the same marks in ordinary highlighted cells are fine.
- **LOW** [dark] right sidebar, This attempt section: Disabled 'Reset board' button in dark mode uses dim grey text/icon on a grey pill; legible but close to the fill. Dimness is partly intentional (disabled at 0 moves), so this is a polish nitpick rather than breakage.
- **LOW** [both] template header (title row): Meta subtitle 'Puzzle 001 · 30 givens · 13-step coached opening' is vertically centered against the large title instead of baseline-aligned, so it floats slightly above the title baseline.

> Pipeline notes: Capture: first shoot.mjs run timed out on page.goto (networkidle, 30s); retry succeeded and produced 2 scroll segments per scheme (4 PNGs), not 3. Gemini call succeeded on first attempt and returned 8 findings, but 6 of 8 did not survive pixel-level verification against 3-4x upscaled crops (claims of invisible pencil marks, inverted dark grid-line hierarchy, illegible empty-state text, clipped selection border, ragged stat alignment, and illegible keypad counts were all overstated or wrong). Overall the template is in good shape in both schemes; remaining items are polish-level.

## marketing-navbar-flyouts (2 confirmed)

- **MEDIUM** [dark] Variant 01 mockup ("Light navbar — centered links, CTA pair"): The variant explicitly titled and described as a light navbar re-themes to a dark background with white text in dark mode (dark.1of3), so the exhibit no longer demonstrates the light-navbar pattern it advertises; it should pin its light styling.
- **LOW** [light] Variant 03 mockup (two-tier navbar / hero boundary): In light mode the white main navbar of variant 03 sits on the pale gray hero with no bottom border or shadow (light.3of3), while variant 01's navbar has a visible bottom divider (light.1of3) — inconsistent edge treatment that makes variant 03 look flat.

> Pipeline notes: Screenshot capture succeeded on first try (6 files, light+dark x 3 segments). First gemini call timed out at 180s (exit 124); retry after 20s succeeded. However, gemini's critique was largely hallucinated: 6 of its 8 findings were disproved by cropped close-up inspection (claimed black-on-dark labels, invisible slider track, disappearing dark-mode CTA, and white-on-pastel badge text are all actually legible with correct token colors; claimed misalignment and padding inconsistencies were not visible). Only 2 findings survived verification. Template is otherwise in good fit-and-finish shape in both schemes.

## deploy-impact-scrubber (4 confirmed)

- **MEDIUM** [both] scrubber header / keyboard hint pill: Keyboard shortcut hint renders the raw KeyboardEvent code string 'ARROWRIGHT' next to the shift glyph instead of a user-friendly symbol like '→' (should read '⇧ →' or 'Shift + Right').
- **MEDIUM** [both] event timeline x-axis: End labels on the scrubber timeline axis overflow the container: the right '20:00' label is cut mid-character by the section edge and the left '12:00' label sits flush/partially clipped against the panel edge. The bottom Traffic chart's axis handles the same labels correctly, so the timeline is missing boundary padding or text-anchor adjustment.
- **LOW** [both] event timeline / playhead: The playhead drag-handle dot half-overlaps the selected event's glyph ring at the top of the stem (black-on-black in light mode, white-on-white in dark), reading as an accidental blob rather than a deliberate marker.
- **LOW** [both] event log (right detail pane): Deploy event titles truncate early with an ellipsis when an impact badge is present ('api@4.18.0 — check…', 'api@4.18.1 — cache l…'), losing meaningful title text; rows without badges show full titles, so the wide badge is squeezing the title.

> Pipeline notes: Pipeline healthy: shoot.mjs produced 4 screenshots (2 light + 2 dark segments) on the first try and the single gemini call succeeded. Gemini returned 8 findings; 5 were dropped after pixel-level verification with enlarged crops: (1) 'black playhead stem invisible in dark mode' is false — the stem renders white in dark; (5) dark event-log metadata is muted but clearly readable, not 'nearly unreadable'; (6) impact-window fill differences across charts are the same orange overlay blending with each series' area fill, not hardcoded inconsistency; (7) the crosshair is correctly masked and does not bleed through the gutters between chart cards; (8) y-axis labels have ~18px padding, not flush. Gemini also mislocated the clipped axis labels (claimed bottom Traffic chart; actually the top timeline axis). One additional defect I found was added (playhead dot/glyph ring overlap).

## coding-terminal-workspace (3 confirmed)

- **MEDIUM** [both] header (template toolbar, command palette pill): The hint label '⌘ Open command palette' and the 'Commands ⌘K' control share one pill with only a single word-space between them, so it reads as one merged string 'Open command palette Commands' and looks like a broken single element.
- **MEDIUM** [light] sessions sidebar (left pane of template): Session subtext lines (e.g. 'zsh · pid 48213', 'pnpm dev · pid 48377') are rendered in very light gray on white in light mode, roughly 3:1 contrast at small size — below AA for body text and noticeably hard to read.
- **LOW** [dark] header (command palette pill keycaps): In dark mode the ⌘ and K keycap badges are dark gray on a dark pill, making them barely visible; light mode renders them clearly, suggesting the keycap background/foreground isn't tuned for dark tokens.

> Pipeline notes: Pipeline healthy: shoot.mjs produced 4 screenshots (2 segments per scheme, not 3) on first try; gemini call succeeded on first try. Of gemini's 8 findings, 6 were dropped on verification: one was a scroll-segment capture artifact (clipped row + selected-row accent bar at the top of segment 2of2), and five were inaccurate on close inspection (status-dot layout is a consistent status-left/activity-right pattern, no white divider through the terminal, dot padding aligns with the Sessions header, search nav uses proper icons, no visible detached tab border). Verified via sips-cropped/upscaled regions read directly.

## poll-survey-builder (4 confirmed)

- **MEDIUM** [both] live preview pane, Q1 active-question block: Unselected radio button outlines for Yes/No are nearly invisible in light mode against the tinted active-question highlight, and are also very dim in dark mode (poll-survey-builder.light.1of3.png, dark.1of3.png). Gemini missed this; verified via zoomed crop.
- **MEDIUM** [both] builder pane, Branch logic section, Add rule button: '+ Add rule' button has very low contrast in both themes: pale-gray text on pale-gray pill in light, dark-gray-on-dark-gray in dark (light.3of3.png, dark.3of3.png). Likely a disabled state, but even for disabled it is barely distinguishable from its background, especially in light mode.
- **LOW** [dark] live preview pane, Q5 open-text placeholder: Dashed border around the open-text answer area ('Respondents type their answer here') is almost invisible against the dark background at normal zoom (dark.2of3.png); light mode is fine.
- **LOW** [both] live preview pane, Q4 ranked items: Rank prefixes (#1–#4) and drag-grip icons are noticeably fainter than the item labels in both themes, receding almost to background level at 1x (light.1of3.png, dark.1of3.png). Legible when zoomed, so a polish nitpick.

> Pipeline notes: Capture and gemini pipeline both succeeded on first try (6 screenshots: light/dark x 3 segments). Gemini returned 8 findings; 4 were dropped after zoom verification: (1) claimed jet-black active-question background in light preview — actually light gray, hallucinated; (2) claimed white-on-yellow 'required' badge — crop shows dark text on amber, legible; (6) Q4/Q5 spacing claim was a screenshot segment-boundary artifact, unverifiable; (7) dashed-box padding imbalance and (8) cramped 'Choose…' dropdowns not visible in zoomed crops — the placeholder has a clear gap before the chevron and appears to be literal 'Choose…' text rather than truncation. One defect gemini missed (faint light-mode radio outlines) was added and confirmed.

## esignature-envelope-flow (2 confirmed)

- **LOW** [both] detail pane - document thumbnails vs preview: Selected Page 1 thumbnail's black selection ring top edge sits ~4-6px above the top edge of the large document preview, creating a slight optical stair-step between the two columns.
- **LOW** [both] footer - Back button: Disabled Back button has quite low contrast (mid-gray text on light-gray pill in light mode, gray-on-dark-gray in dark mode); readable but could be mistaken for a broken state rather than disabled.

> Pipeline notes: Pipeline: shoot.mjs timed out on first attempt (networkidle 30s) but succeeded on retry, producing 2 scroll segments per scheme (4 PNGs), not 3. Gemini critique ran successfully and returned 8 findings, but 6 of 8 failed visual verification against crops and were dropped: Cancel renders white/legible in dark mode; the document preview has proper bottom padding; stepper connector lines are visible in dark mode; the Next button correctly inverts to a light pill in dark mode; the Back button is legible (kept only as a low-contrast nitpick); badge and Initials-box text appear properly centered. The template is generally well polished with correct token-based dark mode (white document paper is intentional).
