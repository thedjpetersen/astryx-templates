# Fit-and-finish review — cycle 9 (2026-07-03)

Pipeline: vite demo server → Playwright headless Chromium (1440x900, light+dark, scrolling segments of `.preview-wrap`) → Gemini designer critique → Claude agent pixel-crop verification. Findings marked unconfirmed did not survive visual verification but were kept as possible nitpicks.
**Totals: 4 high / 18 medium / 25 low confirmed across 12 templates.**


## milestone-burnup (6 confirmed)

- **HIGH** [both] Milestones table (T/S/D and target-date columns): The T, S, D and target-date columns collapse to ~1-character width, so dates like 'Apr 20' and 'Jun 15' wrap vertically one letter per line ('A/p/r/2/0') and numeric values stack; the table is unreadable.
- **HIGH** [both] Milestones table, rightmost Status column: The Status column overflows the card: header is truncated to 'St..' and the circular status badges (D/A/C/A) are clipped in half by the table's right edge in every row.
- **HIGH** [dark] Scope vs. completed chart plot area: In dark mode the chart series have near-zero contrast: the completed area is a dark grey barely distinguishable from the panel background and the total-scope line and forecast band are almost invisible, making the chart look empty/broken (only the red GA-target dashed line reads).
- **MEDIUM** [dark] Milestones table, expanded M2 row work items: The 'In progress' pill keeps a light-blue background with dark-blue text in dark mode (hardcoded light-theme colors), while the adjacent 'Done' pills correctly invert to dark green.
- **MEDIUM** [both] Scope vs. completed chart legend: Legend entries 'Total scope', 'Completed', 'Forecast band (6–7 pts/wk)' are text-only with no color swatches or line-style markers, so labels cannot be mapped to the chart series.
- **LOW** [both] KPI row, Forecast date card: Subtitle '2 wks past target range Aug 10 – Aug 17' wraps awkwardly, orphaning '17' on its own line.

> Pipeline notes: Capture and gemini both succeeded on first try (4 screenshots: light/dark x 2 segments). Two gemini findings were dropped after visual verification: (1) claim that the M3 progress-bar fill is hardcoded black and invisible in dark mode — a zoomed crop shows the fill renders light grey/white and is visible; (2) claim that the scope-change event overlay covers the chart x-axis — the card sits fully below the axis labels, which are unobstructed. Gemini's items 2 and 7 (clipped badges + truncated 'St..' header) were merged into one finding. The dark-mode chart-contrast finding was added by me; gemini only flagged the progress bar.

## mail-compose (5 confirmed)

- **HIGH** [dark] compose window (entire floating card): The compose window does not adapt to dark mode at all: in both dark screenshots it stays a white card with light inputs, black text, and pink error banner while the surrounding mail skeleton correctly turns dark, indicating hardcoded light-theme colors instead of tokens.
- **MEDIUM** [both] compose window footer, Send split button: Send split-button styling is incoherent: the Send label sits in a light-gray pill with low-contrast gray text (reads as disabled) while the adjoining dropdown chevron is a fully saturated solid black circle, so the two halves look like unrelated controls rather than one button group.
- **MEDIUM** [dark] template preview background, 'Mail' skeleton heading: In dark mode the 'Mail' heading of the background skeleton is dark-on-dark and nearly illegible (clearly readable black in light mode), suggesting a hardcoded text color on the skeleton header.
- **MEDIUM** [both] compose window attachments row, metrics-snapshot.png upload: The upload progress indicator is a thick solid-black fill over a barely visible hairline track, visually much heavier than every other element in the card and reading as a stray black bar rather than a progress bar.
- **LOW** [both] compose window, To recipient row: The 'To' label is vertically centered against the two-row recipient field instead of aligning with the chip row, so it sits visibly below the chips' baseline in all screenshots.

> Pipeline notes: Pipeline healthy: shoot.mjs produced 2 segments per scheme (4 files) on first try; gemini critique succeeded on first call. Two gemini findings were dropped after visual verification: alleged uneven window-control icon spacing (gaps look even) and alleged cramped margin above the error banner (spacing above/below looks comparable). Screenshots at /tmp/astryx-uiux/shots/mail-compose.{light,dark}.{1of2,2of2}.png.

## deck-review-comments (3 confirmed)

- **MEDIUM** [both] slide stage (Scale pricing card): Annotation marker 2 is positioned directly on top of the 'Scale $129' heading, partially covering the '$' glyph; the pin collides with slide text instead of sitting on an edge or whitespace.
- **LOW** [both] comment thread panel (reply composer): The reply textarea shows a native browser resize grip in its bottom-right corner, letting users drag-resize it inside the fixed-width comment card; looks unpolished and can break the card layout.
- **LOW** [dark] comment thread panel (metadata text): Timestamps, reply counts ('1 reply', '0 replies'), and 'Replying as Maya Chen' use a fairly dim gray on the dark card background; legible but borderline contrast for small text in dark mode.

> Pipeline notes: Capture succeeded on first try (2 scroll segments per scheme, 4 PNGs). Gemini call succeeded but 6 of its 8 findings were hallucinated and were dropped after pixel-level crop verification: the active-thumbnail ring correctly inverts to white in dark mode (not invisible, not clipped), the @Jordan mention is light blue on navy and readable, the filter toggle and 'Replying as' text are legible light gray, nested-reply dimming is intentional hierarchy, and the white 'In review' badge in dark mode matches the template's consistent token inversion (same as the #1/#2 badges and All pill). The template's dark mode is well tokenized overall. The marker-overlap finding corresponds loosely to gemini's observations; the textarea resize grip was found during my own review. Verified using zoomed crops via /tmp/astryx-uiux/crop.mjs (outputs in /tmp/astryx-uiux/crops/drc-*.png).

## video-clip-timeline (6 confirmed)

- **MEDIUM** [both] right inspector panel — Video Opacity slider: Slider thumb collides with the "100%" value label: there is zero gap and the thumb visually overlaps the first glyph of the label in both schemes, reading as a layout collision.
- **LOW** [both] right inspector panel — Speed field: Value "1" is left-aligned while the small "x" glyph is pinned flush to the opposite (right) edge of the input; the extreme separation makes it read as a clear button rather than the unit "1x".
- **LOW** [both] timeline ruler / scrubber: The red playhead line slices straight through the center of the circular scrubber thumb and continues up over the ruler tick labels, looking like a z-index/rendering artifact rather than an intentional handle.
- **LOW** [dark] timeline header — zoom segmented control (50%/100%/200%): In dark mode the active "100%" pill background is nearly the same grey as the panel behind it, so the selected state is barely distinguishable; light mode shows a crisp white pill, indicating an inconsistent dark-mode token.
- **LOW** [dark] left tool rail: In dark mode the circular background marking the active pointer tool is only faintly lighter than the rail background, failing to clearly communicate the selected tool (clear in light mode).
- **LOW** [both] timeline audio tracks A1/A2: Waveform bars sit flush against the bottom border of the audio clips with zero bottom padding while there is ample padding above the label, and the end bars square off against the clips' rounded corners, looking clipped.

> Pipeline notes: Pipeline healthy: shoot.mjs produced 4 screenshots (light/dark x 2 scroll segments) on first try; gemini critique succeeded on first call. Gemini returned 8 findings; 3 were dropped after pixel-level verification via sips crops: clip-label padding (labels have visible ~6-8px left padding), missing slider filled-state (Gain slider clearly shows filled vs unfilled track), and erratic toolbar spacing (gaps look even). The most severe confirmed issue — the Opacity thumb overlapping its \"100%\" label — was missed by gemini and added during verification. Screenshots at /tmp/astryx-uiux/shots/video-clip-timeline.*.png.

## cpu-flame-graph (4 confirmed)

- **MEDIUM** [both] right rail — Hottest under root card: The label 'crypto.verifySignature' runs to within a pixel or two of its '230ms' value — no minimum gap, flex-gap, or truncation on the label, so long frame names visually collide with the time column.
- **MEDIUM** [dark] flame graph control bar — Compare baseline toggle: In dark mode the toggle thumb is nearly the same dark gray as its track, making the switch state almost unreadable; the light-mode version (white thumb on gray track) is clearly legible.
- **MEDIUM** [both] flame graph header — breadcrumb pill: The active-root breadcrumb pill is truncated to '(profile' with no ellipsis and a dangling open parenthesis (full name is '(profile root)'), which reads as a rendering error in both schemes.
- **LOW** [both] flame graph control bar — search input: Placeholder text is clipped mid-word to 'Search frames (e.g. pg.Clie' at the input's default width, with no ellipsis; the example string is too long for the field.

> Pipeline notes: Pipeline healthy: shoot.mjs produced 4 shots (2 scroll segments per scheme) on first try; gemini responded on first call. 4 of gemini's 8 findings were rejected on visual verification: (a) claimed dark.2of2 rendered in light mode — false, it renders dark; (b) claimed hardcoded light tokens on the '(profile'/'depth 0' pills in dark mode — these are a consistent primary-token inversion of the light theme's black pills, not hardcoded colors; (c) claimed asymmetric right-rail padding — padding looks balanced in enlarged crops; (d) claimed top-aligned flame node labels — labels are vertically centered. The partially clipped 'CPU Flame Graph' card title in the 2of2 segments is a scroll-capture artifact, not a template defect.

## layout-primitives-gallery (4 confirmed)

- **MEDIUM** [light] cards panel — list/flat-gaps (also visible in cards panel dotted canvas): list/flat-gaps rows in light mode have a translucent, near-white fill with no border or shadow; the row edges are barely distinguishable from the dotted canvas, undermining the caption's premise that 'gaps do the separating'
- **LOW** [dark] cards panel — card/gray-body caption: Caption next to the card/gray-body chip is hardcoded copy reading 'White header, muted body wash.' which contradicts the dark-mode rendering directly below it, where the header band is dark, not white
- **LOW** [both] lists panel — section subtitle vs list variants: Subtitle claims 'Four ways to frame the same rows' but the datasets differ: list/divided shows 5 people (incl. Sana Iqbal), list/card-rows and list/flat-gaps show only 4, and list/sticky-groups uses a different roster (Ada Lin, Andre Silva, Bea Kowalski)
- **LOW** [both] lists panel — surface fills (flat-gaps, sticky-groups vs divided, card-rows): Inconsistent surface opacity: the dotted canvas pattern shows through flat-gaps rows and sticky-groups rows/letter headers (translucent fills) while list/divided and list/card-rows surfaces are opaque, making some variants look unfinished next to others

> Pipeline notes: Capture and gemini both succeeded on first try. However, the 3 scroll segments only cover sections 01 containers, 02 cards, and 03 lists; the '04 Media object' heading is clipped at the very bottom of segment 3of3, so the media and dividers panels (sections 04–05) were never captured or reviewed. Gemini returned 8 findings but 5 were refuted on pixel-level verification: (1) dark-mode text in card/well and card/gray-body is light gray on dark and clearly readable; (2) the edge-to-edge toggle does work — the preview card loses radius/side borders inside the 320px frame; (5) container content-width math is correct given the slider position (~640px: min(640, column) − 2×padding matches all four readouts); (6) sticky-groups avatars are the same size as other list variants; (7) Discard/Publish footer actions are vertically aligned. Screenshots at /tmp/astryx-uiux/shots/layout-primitives-gallery.{light,dark}.{1,2,3}of3.png.

## subscription-billing-portal (3 confirmed)

- **MEDIUM** [both] Invoices table (template preview): In the Failed invoice row, the download icon is pushed ~85px right of the download-icon column used by every other row because the Retry button displaces it inline instead of the icon staying in a fixed column; the icon column loses vertical alignment.
- **LOW** [both] Usage this cycle card: Storage meter fill is neutral (black in light mode, white in dark mode) while Seats and API requests meters use brand yellow; if this is threshold-based near-limit coloring it has no legend, so it reads as an inconsistent/hardcoded fill.
- **LOW** [both] Invoices table Amount column: Currency amounts ($99, $128.40, $29) are left-aligned; financial figures are conventionally right-aligned for scanability.

> Pipeline notes: Pipeline healthy: shoot.mjs produced 4 screenshots (2 segments x 2 schemes) on first try; gemini critique succeeded on first call. However 5 of gemini's 8 findings were dropped after zoomed-crop verification: its top two contrast claims (AMEX row text illegible in dark mode; Refunded pill white-on-light-grey) were hallucinations — both render with correct, legible token colors — and the Active-pill right-margin, Storage-bar-thickness, and Failed/Retry height-mismatch claims were not visible at 3x zoom (heights differ by design with aligned centers). No additional defects found beyond gemini's confirmed ones.

## shared-team-inbox (4 confirmed)

- **MEDIUM** [both] template card header (help@brightpath.io row): When the page is scrolled (segment 2of2), the template's header text 'help@brightpath.io', the '5 open' badge, and the avatar stack are sliced horizontally mid-glyph at the top edge of the preview card; the header is not sticky and has no scroll masking, so the scrolled state looks broken.
- **MEDIUM** [both] reply composer footer: The caption 'Sends from help@brightpath.io to Ana Lucia Reyes.' wraps awkwardly, orphaning 'Reyes.' onto a second line with a large line gap; the mail icon centers against the two-line block, making the footer row look disjointed next to the Send reply button.
- **LOW** [dark] ticket list (left column of template): In dark mode the 'Unassigned' chips (Riley Moss, Nina Kowalski rows) use muted gray text on a dark gray pill; contrast is noticeably lower than surrounding text and hard to read at a glance.
- **LOW** [dark] internal note block (thread view): In dark mode the internal note's muted caption 'Only visible to the help@ team.' is gray on the olive note background, dimmer than the body text and borderline low-contrast (body text itself is legible, contrary to gemini's 'illegible' claim).

> Pipeline notes: Pipeline healthy: shoot.mjs produced 4 shots (2 segments per scheme, not 3) on first run; gemini succeeded on first call with 8 findings. 4 gemini findings were dropped after zoom verification: (1) dark-mode internal note is olive with legible light text, not light-yellow-with-white-text as claimed (downgraded to the caption-contrast low finding); (2) the white 'Open' chip in dark mode is a consistent design-system inversion (matches the inverted 'All' tab pill), not a hardcoded color; (3) email truncation is not premature — only ~13px of space remains before the Open chip; (4) '5 open' badge and search-icon vertical alignment both look correctly centered at 4x zoom. Dark-mode tag chips (bug/csv-export) were also checked and are properly dark-themed. Screenshots at /tmp/astryx-uiux/shots/shared-team-inbox.{light,dark}.{1,2}of2.png.

## region-tile-cartogram (6 confirmed)

- **MEDIUM** [dark] tile grid header hint: Helper text reads 'Tap a tile to pin · darker = more' in dark mode, but the dark-mode color ramp is inverted so lighter/brighter blue = higher value (GA 64% is bright blue, MS 32% is near-black navy; legend shows 74.2%-88.0% as the lightest swatch). The copy is hardcoded for the light ramp and gives actively wrong guidance in dark mode.
- **MEDIUM** [both] quantize legend: Lowest-bucket swatch (19.0%-32.8%) is nearly invisible in both schemes: near-white square on the white panel in light mode, near-black navy square on the dark panel in dark mode. No border/outline to keep the extreme ramp step visible.
- **MEDIUM** [both] ranking list (bottom rows) and tile grid: Bars for bottom-ranked rows use the lowest-bucket ramp color and have almost no contrast with their track: rows 49 MS 32.0% and 50 WV 30.0% show an effectively empty-looking bar in light mode, and only a faint fill in dark mode. The WV 30.0% tile in the grid similarly nearly disappears against the dark background.
- **LOW** [dark] metric tabs and year transport: In dark mode the active segment ('Adoption' tab and '2026' year pill) uses a background darker than the inactive strip, inverting the usual elevation cue; selection reads mainly via the thin border and text weight, looking dimmer than unselected options.
- **LOW** [both] main column below legend: Because the 50-row ranking column sets the card height, the main column is a large blank region (roughly 600px of empty panel) below the quantize legend in scroll segment 3 of both schemes, making the lower half of the template look unfinished.
- **LOW** [both] ranking list index column: Rank numbers are left-aligned, so single digits (1-9) and double digits (10-50) do not share a right edge; conventional right-aligned/tabular numerals would look cleaner. State codes remain aligned, so impact is minor.

> Pipeline notes: Pipeline healthy: shoot.mjs produced 6 screenshots on first try and the single gemini call succeeded. However, gemini's four most severe findings were false positives and were dropped after visual verification: (1) the claimed HI/TX/FL container overflow is just the scroll-segment boundary (light.2of3 shows the tiles inside the container border); (2) tile text is not hardcoded white — pale tiles use dark blue text in light mode and legibility is fine; (3) the TX sparkline 'gray bar' is a consistent per-value color ramp used identically in the CA card; (4) dark-mode ranking track pills are visible on unselected rows. The 'darker = more' copy contradiction in dark mode was verified via zoomed crops of both schemes and added as the top finding. Crops used for verification are in /tmp/astryx-uiux/shots/crop_*_3x.png.

## subtitle-cue-editor (1 confirmed)

- **MEDIUM** [dark] Cues table header — 'Show issues only' toggle (right side of template preview): In dark mode the toggle knob renders near-black on a dark gray track, so the knob is barely distinguishable and the on/off state is hard to read; light mode correctly uses a white knob on a gray track (relationship is inverted in dark).

> Pipeline notes: Pipeline healthy: shoot.mjs produced 4 screenshots (2 segments x light/dark) on first try; gemini critique succeeded on first try and returned 8 findings. However, 7 of 8 gemini findings were disproven by high-zoom crops (claimed invisible stepper icons, unreadable green banner, dark text on red pill, invisible dropdown chevron, hardcoded white 'At playhead' pill, stepper misalignment, cramped title spacing — none visible in the actual pixels; several elements gemini called broken are correctly token-themed inversions). Only the dark-mode toggle knob contrast survived verification. My own sweep (transport bar, slider, timeline strip, footer, table pills) found no additional defects. Screenshots: /tmp/astryx-uiux/shots/subtitle-cue-editor.{light,dark}.{1of2,2of2}.png; verification crops in /tmp/astryx-uiux/crops/sce-*.png.

## error-404-collection (2 confirmed)

- **LOW** [both] template card header (variant switcher row): Unlabeled yellow status dot floats with a noticeable gap to the left of the 'Minimal / Lost robot / Helpful links / Incident' segmented control; it reads as an orphaned, disconnected element since nothing associates it with a tab or state.
- **LOW** [dark] template card header (variant tabs and 404/500 toggle): In dark mode the unselected tab labels ('Lost robot', 'Helpful links', 'Incident') and the unselected '500' toggle option are dim gray on a dark pill background — readable but on the low side of comfortable contrast, weaker affordance than the light-mode equivalent.

> Pipeline notes: Capture succeeded on first try (2 scroll segments per scheme, not 3). Gemini call succeeded and returned 8 findings, but pixel-level zoom verification disproved most of them: description text, footer, mock-browser error-code line, URL text, and the 'showing 404' badge in dark mode are all legible muted-token grays (gemini called them 'nearly illegible'); the '4 variants' pill is correctly vertically centered with the heading. The half-clipped card header visible in the 2of2 segments is the gallery's sticky toolbar overlapping during a mid-scroll capture — gallery chrome, so excluded per instructions. The misspelled URL 'launch-blog-psot' is intentional 404 fixture data, not a defect.

## issue-detail (3 confirmed)

- **LOW** [both] detail pane header (byline under issue title): Inconsistent metadata separators in the byline: 'Opened by Priya Nair' and 'Jun 24, 2026, 2:15 AM' are separated only by whitespace, while the following items ('· Sprint 24 · Jun 22 – Jul 3') use middots — the first gap reads like a formatting error.
- **LOW** [both] Details sidebar, Estimate row: The Estimate value '5 pts' is rendered as a monospace pill while every other Details value (Assignee, Reporter, Sprint, Watchers) is plain sans-serif text; the mono/pill treatment is otherwise reserved for code-like tokens (issue key, branch names), so '5 pts' visually clashes.
- **LOW** [both] Description steps and activity comments: Inline code is shown with raw literal backticks instead of styled code spans: '`slow-webhook`' in step 1 of the description and '`payments.debug`' in Sam Okafor's comment, even though the template styles other code tokens (branch names) as mono chips.

> Pipeline notes: Pipeline healthy: shoot.mjs produced all 6 screenshots on first try and gemini responded on first call. However, 5 of gemini's 8 findings were disproven on zoomed verification crops (claimed white-on-pastel text on label chips and @Maya mention — actually tinted token text with good contrast; claimed Send button covers the textarea resize handle — button is below the textarea; claimed Watch icon misalignment and tight label-row wrapping — both fine). One kept finding (backticks) was added by me, not gemini. Screenshots: /tmp/astryx-uiux/shots/issue-detail.{light,dark}.{1,2,3}of3.png; verification crops in /tmp/astryx-uiux/shots/crops/idet/.
