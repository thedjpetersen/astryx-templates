# Fit-and-finish review — cycle 3 (2026-07-03)

Pipeline: vite demo server → Playwright headless Chromium (1440x900, light+dark, scrolling segments of `.preview-wrap`) → Gemini designer critique → Claude agent pixel-crop verification. Findings marked unconfirmed did not survive visual verification but were kept as possible nitpicks.
**Totals: 6 high / 17 medium / 28 low confirmed across 12 templates.**


## day-planner-timeblock (5 confirmed)

- **HIGH** [both] schedule column, 3:30 PM 'Pharmacy + grocery run' block: Block title text overflows above the 30-minute block's top edge, so the block's orange top border (shared with the overlap-group outline) cuts straight through the words 'Pharmacy + grocery run', making the title look struck-through/broken even though the task is unchecked.
- **MEDIUM** [both] schedule column, completed blocks (7 AM 'Deep work — Q3 proposal draft', 9 AM 'Admin sweep — email + calendar'): Completed-block titles use dim gray strikethrough text on a dimmed block background with very low contrast; in dark mode the 9 AM title is barely legible against the near-black block.
- **LOW** [both] schedule column, 2–4 PM overlap group: The outer orange overlap-group outline and the inner event blocks' own orange borders double up, producing parallel double lines along the shared edges and small unpolished corner gaps where the two rounded rects don't nest flush.
- **LOW** [both] backlog column, 'Read distributed-systems paper' row: The long task title runs nearly flush against the circular '+' add button (only a few px gap), breaking the comfortable right-side spacing every other backlog row has; no truncation or min-gap is applied.
- **LOW** [light] daily shutdown panel, 'Save journal' button: The disabled 'Save journal' button is very faint — pale gray text/icon on a pale gray pill in light mode — sitting near the edge of legibility; dark mode is dim but readable.

> Pipeline notes: Capture failed on first attempt (page.goto networkidle 30s timeout) but succeeded on retry; produced 2 scroll segments per scheme (not 3). Gemini critique succeeded on first call and returned 8 findings; 3 were dropped after pixel verification (Overlap badge does not obscure the subtitle, short-block metadata hiding is graceful responsive behavior, Conflicts badge icon/number alignment looks correct in crops). Gemini's 'grid line strikes through Pharmacy text' finding was confirmed but re-attributed to the block's own top border after cropping. Verification crops saved under /tmp/astryx-uiux/shots/crops/ (l-/d- prefixed files).

## bezier-easing-studio (2 confirmed)

- **HIGH** [both] transport bar (bottom of template): At t=0 the timeline scrubber handle sits directly on top of the 't 0 / 60' timecode, covering roughly half of the final '0'; the track's left edge starts under the text instead of after it.
- **MEDIUM** [both] Curve column, cubic-bezier graph: Both curve endpoint anchors are clipped to half-shapes by the plot boundary: the origin handle at bottom-left renders as a half-circle cut by the left axis, and the end anchor at the top-right is likewise cut in half at the plot's right border.

> Pipeline notes: shoot.mjs timed out on first run (page.goto networkidle, 30s) but succeeded on retry, producing 2 scroll segments per scheme (not 3). Gemini call succeeded on first attempt and returned 8 findings, but 6 were dropped on visual verification: active toggle text is legible in both schemes, presets grid rows have a normal ~10px gap, Stage helper text respects card padding, J/K keycap glyphs are centered, badge icon spacing is normal, and the faint Opacity pill is the intended opacity-0.00 demo state at t=0, not a color-token bug.

## workout-session-logger (4 confirmed)

- **HIGH** [both] Exercise card header (Cable Triceps Pushdown, middle column): The Swap button wraps onto its own left-aligned row below the title/subtitle and the '0/3 sets' pill jumps to the far right edge, while every other exercise card (Bench Press, Incline Dumbbell Press, Dumbbell Lateral Raise) keeps the pill next to the title and the Swap button right-aligned in the header row. Visible in light/dark 2of3 and 3of3.
- **LOW** [both] Routine list (left column of template): 'Cable Triceps Pushdown' wraps to two lines ('Cable Triceps' / 'Pushdown') while the similarly long 'Dumbbell Lateral Raise' fits on one line, breaking the list's vertical rhythm and shifting the radio circle's alignment relative to the title. Visible in light/dark 1of3.
- **LOW** [both] Set tables (middle column exercise cards): Dashed 'PREV' ghost value boxes use a noticeably smaller corner radius than the adjacent Weight/Reps/RPE inputs, which are much rounder; adjacent elements in the same row use two different radius languages. Visible in all table rows, light and dark.
- **LOW** [both] Projected 1RM list (right column): The '+3 lb' delta badge sets white text on a medium green fill (~3.5:1), below WCAG AA for small text; it is readable but the lowest-contrast text in the panel. Gemini called it unreadable neon green, which is overstated. Visible in 1of3 both schemes.

> Pipeline notes: shoot.mjs timed out on the first run (page.goto networkidle 30s) but succeeded on the mandated retry; all 6 screenshots captured. Gemini critique succeeded on the first call and returned 8 findings; 4 were dropped after visual verification: 'conflicting active states' (no highlight on the Bench Press card), 'no prior badges near-black in light mode' (they are light gray with dark text), 'massive dead space right of tables' (inputs end near the card edge), and 'cramped PREV/WEIGHT gap' (column gaps are uniform ~8-10px). Dark mode appears fully tokenized with no hardcoded-color leaks. Crops used for verification are in /tmp/astryx-uiux/shots/crop-*.png.

## nonogram-pixel-reveal (4 confirmed)

- **HIGH** [both] preview panel top / right info column (scrolled state): When the page is scrolled (segment 2of2), the right column heading 'Picture progress' and its '0%' value are clipped mid-glyph at the top edge of the preview area, while the left board toolbar (Fill/Cross/Hint/Restart) remains sticky and fully visible — the right column lacks matching sticky positioning or a scroll mask, so text is chopped in half.
- **MEDIUM** [dark] right info column — Picture progress bar: In dark mode the progress bar track is rendered at rgb(23,23,26) on a rgb(31,31,34) panel background (~1.06:1 contrast), making it effectively invisible at 0%; light mode uses a clearly visible rgb(243,246,247) track. Suggests the track color is not properly tokenized for dark mode.
- **LOW** [both] board toolbar (Fill/Cross/Undo/Hint/Restart row): Inconsistent button styling in the same toolbar: 'Hint' has a permanent pill background making it look active or primary, while 'Restart' is a plain text button and Undo is an icon-only circle — three different affordances for peer actions.
- **LOW** [both] board card / grid bottom framing: The gap between the last grid row and the board card's bottom border (~12-15px) is noticeably tighter than the left and top interior padding, making the bottom framing of the grid feel cramped and asymmetric.

> Pipeline notes: shoot.mjs failed on first run (page.goto networkidle 30s timeout) but succeeded on the mandated retry, producing 2 scroll segments per scheme instead of 3. Gemini critique succeeded on first attempt and returned 8 findings; 4 were dropped during verification: 'column clues scroll out of view' (the full grid including clues is visible in segment 2), 'cross-column baseline misalignment' (not visibly present), 'severe low-contrast sidebar text in dark mode' (crop shows readable gray text; the real dark-contrast defect is the progress track, added as a verified finding via pixel sampling), and 'inverted 0/62 cells pill too dominant' (matches the gallery app's own inverted-pill idiom, appears intentional). Gemini's findings 1 and 3 were merged into the single confirmed top-clipping/sticky-mismatch finding.

## query-plan-profiler (6 confirmed)

- **HIGH** [both] Plan tree (middle column, EXPLAIN plan card): Seq Scan row's right-aligned cost value overflows the card and is clipped mid-character (renders as '44,' cut off at the card border); the indented rows do not respect the container's right padding used by the header ('total cost 48,200').
- **MEDIUM** [both] Plan tree (middle column, Sort row): Stray truncated text 'S.' appears between the Sort badge and '~18,432 rows' — looks like a leftover/truncated node label and reads as a rendering glitch.
- **MEDIUM** [both] Slow queries list (left column): Stat rows wrap inconsistently: items 1 and 3-6 break the calls stat across two lines ('12.4k' on the top line with 'calls' orphaned below, appearing to swap label/value rows), while item 2 ('Daily revenue rollup') fits all stats on one line — the list looks ragged and broken.
- **LOW** [both] Index suggestions card (right column): 'high impact' and '-92% cost' badges use white text on saturated green, giving roughly 2.5-3:1 contrast (below WCAG AA for small text); the identical green in both themes also suggests a hardcoded color rather than a token.
- **LOW** [dark] EXPLAIN plan controls (middle column): In dark mode the 'Compare plans' toggle thumb is nearly the same dark grey as the track, making the off state barely visible; the light-mode version (white thumb on grey track) is fine.
- **LOW** [both] Index suggestions (right column, below card): The helper paragraph ('Applying swaps in the planner's rewritten plan...') floats unboxed beneath the suggestion card, visually detached from the 'Apply index' action it explains.

> Pipeline notes: Pipeline healthy: shoot.mjs produced 4 shots (2 light + 2 dark; page needed only 2 scroll segments, and 2of2 is nearly identical to 1of2 since the page barely scrolls). Gemini responded on the first call with 8 findings. Two were disconfirmed on zoom-in and dropped: the 'Limit' badge is a properly adapted light-grey pill with dark text in light mode (not a hardcoded dark remnant), and the light-mode toggle thumb is white, a normal off state — however the toggle contrast issue is real in dark mode, reported above. Gemini's findings 1 and 2 (swapped stat labels + broken item 2 layout) share one root cause (inconsistent stat-line wrapping) and were merged into a single finding. Verification crops saved at /tmp/astryx-uiux/shots/crop-light.png, crop-dark.png, crop2-light.png, crop2-dark.png.

## expense-approval-queue (4 confirmed)

- **HIGH** [both] detail pane, Receipts section: The 'Receipts (5 of 5)' section renders no content: the heading is immediately followed by the footer action bar with zero receipt tiles, despite the template description promising receipt tiles and the heading claiming 5 items. Visible in all four screenshots including the fully scrolled 2of2 segments, so the tiles are either not rendering or are clipped/hidden behind the sticky footer.
- **LOW** [dark] detail pane, policy warning banner: In dark mode the banner subtext ('Team dinner is $412.80 for 4 attendees...') is a muted warm gray on an olive-brown background — noticeably lower contrast than the bright-white 'Policy warning' title and dimmer than its light-mode counterpart. Legible at zoom but borderline for accessibility.
- **LOW** [both] queue list, selection controls: The bulk-select controls ('Select all clean (3)' plus per-row selectors) are perfect circles that read as radio buttons rather than checkboxes, an ambiguous affordance for a multi-select bulk-approve interface. (Gemini's additional claim that the select-all control is misaligned with the row controls is false — they are aligned within ~2px.)
- **LOW** [both] detail pane, footer action bar / card bottom: In the fully scrolled 2of2 segments, the Reject/Approve action bar sits ~85-90px above the bottom edge of the template card, leaving a band of dead empty space beneath it (the queue/detail divider also stops short), which makes the bottom of the template look unfinished. May be a min-height on the preview container rather than the template itself.

> Pipeline notes: Capture succeeded on first try (2 scroll segments per scheme, 4 PNGs). Gemini call succeeded on first try but had low precision: 5 of its 8 findings were contradicted by zoomed crop inspection (badge collision on Jonah Fields — clear gap exists; dark 'Over limit' badge described as muddy dark-red — actually solid red pill with white text matching the queue badges; footer helper text misalignment — it is vertically centered; light-mode Approve button described as ghost/outline — it is solid black with white text; divider padding asymmetry above Report total — spacing is roughly even). Those were dropped. The dead-space-below-footer finding was added by my own inspection.

## podcast-word-splicer (4 confirmed)

- **MEDIUM** [both] transcript / Edits-Chapters side panel: The Edits/Chapters side panel is not sticky, so in scroll segments 2of3 and 3of3 the entire right column is a large empty void with no visible divider next to the transcript, making the layout look unbalanced and broken for most of the page.
- **MEDIUM** [both] bottom audio player controls: At playhead 00:00 the circular slider thumb overlaps the elapsed-time label, covering the last digit so it reads "00:0(thumb)" — the label and slider have no horizontal gap reserved for the thumb at its minimum position (visible in light/dark 3of3).
- **MEDIUM** [both] bottom audio player waveform: The two-lane waveform container renders completely empty (no bars, blocks, or mirrored word spans) even though the legend above it says words/cut spans are mirrored on a shared time axis, leaving a large blank gray box that looks unfinished (light/dark 3of3).
- **LOW** [both] bottom audio player waveform: The playhead vertical bar sits flush against the waveform container's left edge and runs full height, crossing through the rounded top-left and bottom-left corners instead of being inset from the border radius (light/dark 3of3).

> Pipeline notes: Capture: first shoot.mjs run failed with a Playwright networkidle timeout; retry succeeded (6 screenshots, 3 segments x light/dark). Gemini critique ran successfully and returned 8 findings, but only a subset survived visual verification: I confirmed the empty right column (its finding 2) and reinterpreted its player-controls misalignment claim (finding 4) as the real defect, thumb-over-label overlap. Dropped after inspecting enlarged crops: dark-mode legend "severe contrast failure" (text is legible gray), dark side-panel/badge contrast (readable), speaker timestamp baseline misalignment (aligned), filler underline striking descenders (normal underline offset, looks fine), missing bottom padding above player (adequate gap), and square bottom corners on the waveform box (all four corners are rounded). I added two defects gemini missed: the empty waveform container and the playhead bar crossing the rounded corners. Light/dark parity is otherwise good — colors appear token-based with no hardcoded-color inversions spotted.

## api-request-explorer (5 confirmed)

- **MEDIUM** [both] collection tree footer: Footer caption is truncated mid-word to 'Synced from workspace fixtures · read-only colle…' in every screenshot; the string never fits its container so the static label is permanently cut off.
- **MEDIUM** [both] request builder header (right of Send button): The 'Save' button is extremely faint in both themes (light gray on near-white in light, dim gray on dark in dark); it reads as ambiguous — could be disabled or broken — and fails contrast either way.
- **LOW** [dark] params list, 'cursor' row toggle: The unchecked toggle circle on the 'cursor' row is barely visible in dark mode (very dark ring on dark background), making the enable/disable affordance nearly undiscoverable; light mode ring is fine.
- **LOW** [both] compiled URL preview bar: Right-aligned metadata '· staging · 2 headers' is dim gray on a near-black bar in both themes; the separator dots are almost invisible and the text sits well below comfortable contrast.
- **LOW** [light] response empty-state pane: Response pane (and the URL preview bar) stay hardcoded near-black in light mode; placeholder text 'Press Send to fire…' is mid-gray on black — legible but low contrast, and the dark-on-light block reads as a possible unthemed element rather than deliberate console styling.

> Pipeline notes: Capture: first shoot.mjs run timed out on page.goto (networkidle); retry succeeded, producing 2 scroll segments per scheme (not 3). Gemini call succeeded on first try, returned 8 findings; 5 were dropped after pixel-level verification with cropped/zoomed regions (claims of staggered tree labels, mixed checkbox/radio controls, selection-highlight bleed, chevron misalignment, and an illegible base URL were all contradicted by the screenshots — labels are column-aligned, all toggles are circular, the selected pill is fully rounded, and the URL uses legible syntax colors). The unchecked-toggle dark-mode faintness finding was added by my own inspection. Screenshots: /tmp/astryx-uiux/shots/api-request-explorer.{light,dark}.{1of2,2of2}.png.

## sql-query-workbench (5 confirmed)

- **MEDIUM** [light] code editor pane: Editor pane background is hardcoded near-black in both schemes; in light mode this creates a harsh pitch-black block under the white tab bar and white surrounding chrome, and the active tab's underline visually merges into the editor's top edge instead of the tab reading as connected to the pane.
- **LOW** [dark] connection panel (left column of the workbench): The connection host caption 'pg-replica-2.internal.test:5432' is low-contrast mid-gray on near-black in dark mode; legible when enlarged but well below comfortable contrast at 1x.
- **LOW** [both] workbench status bar (footer): Status bar repeats the same connection name on both ends: left shows 'idle · analytics-prod (read replica)' and right shows 'analytics-prod (read replica) · shopdb' — redundant duplication of the environment string.
- **LOW** [both] schema / saved-queries section headers: Inconsistent count-badge styles between sibling sections: 'Schema' uses an elongated pill with text ('3 tables') while 'Saved queries' uses a small filled circle with just '3'.
- **LOW** [both] results pane header: The 'Results' heading is noticeably larger and bolder than the sibling panel headers 'Connection', 'Schema', and 'Saved queries', giving the panes inconsistent heading typography.

> Pipeline notes: Capture: first shoot.mjs run timed out on page.goto (networkidle, 30s); retry succeeded and produced 2 scroll segments per scheme (4 files) instead of 3. Gemini critique succeeded on first call and returned 8 findings; 4 were dropped after pixel-level verification (timestamptz pill padding is fine, editor has ~16px left padding not zero, '+' tab icon is vertically aligned with the close icons, and the sidebar/editor divider offset is expected for independent panes). Gemini's 'nearly illegible' dark-mode connection string was downgraded to low — it is dim but readable. The 'Results' heading typography finding is my own addition. Verification crops saved under /tmp/astryx-uiux/shots/crop_*.png.

## live-auction-room (8 confirmed)

- **MEDIUM** [both] artwork header metadata pills: All three metadata pills below the 'Harbor Dusk at Kestrel Bay' title truncate with ellipses ('Edda Voss (1918-19...', 'Oil on canvas, 1...', '61 x 46 cm, signed lower ri...'), hiding artist dates, medium year, and signature info even though the full row width and vertical space are available for wrapping.
- **MEDIUM** [both] playback bar scrubber: At position 0:00 the scrubber thumb overlaps the trailing '44' of the '0:00 / 1:44' timecode text; the thumb circle sits on top of the last digit instead of the track starting after a gap.
- **MEDIUM** [dark] In the room avatars: In dark mode the avatar initials (MO, DR, IK, FA, Y) render as dim gray text on dark charcoal circles — very low contrast and hard to read; light mode initials are comfortably legible, suggesting the initials color does not adapt to the scheme.
- **MEDIUM** [both] Catalog notes section: The Provenance value truncates to one line with an ellipsis ('Private collection, Oslo; acquired directly fr...') in a detail-reading section where every other field wraps/fits; the text should wrap to additional lines.
- **LOW** [dark] playback bar speed toggles: The active '1x' speed toggle uses a near-black pill inside a lighter gray track in dark mode, inverting the elevation cue used in light mode (white raised pill) so the selected state reads as recessed/darker than its siblings.
- **LOW** [both] Place a bid card, jump bid row: The '+' increment button sits only a few pixels from the 'Place jump bid' button while the stepper has generous internal spacing on its left side, making the row spacing look uneven and the two controls appear to belong to one group.
- **LOW** [both] Proxy bidding card: The Proxy bidding card is stretched to match the taller Place a bid card but its content is top-aligned, leaving a noticeable empty void at the bottom of the card.
- **LOW** [both] right column section headers: 'Bid history' and 'In the room' headers have leading icons (gavel, people) but the 'Catalog notes' header is text-only, breaking the established header pattern in the same column.

> Pipeline notes: Pipeline healthy: shoot.mjs produced 4 screenshots (2 scroll segments per scheme, not 3) on first try; gemini critique succeeded on first call. All 8 gemini findings verified via 3x zoomed crops (saved as /tmp/astryx-uiux/shots/z_*.png). Two corrections to gemini's claims: (1) avatar initials sit on filled dark circles, not transparent backgrounds — low contrast confirmed but mechanism differs; (2) the 'tight timecode spacing' finding is actually the scrubber thumb overlapping the last digit of the timecode, a clearer defect. No high-severity/broken elements found; template renders consistently between schemes otherwise.

## fleet-dispatch-board (3 confirmed)

- **MEDIUM** [both] driver timeline lanes (middle panel): Vertical hour grid lines render on top of the stop chips instead of behind them — a hairline crosses straight through the S-021 chip (and other chips) from top to bottom; in dark mode the light gray line over the amber chip is very noticeable. Z-index/layering bug.
- **MEDIUM** [both] timeline header and right edge of lanes (middle panel): The last hour label is clipped mid-glyph to "10:0" at the right edge of the timeline viewport, and the S-022 chip is hard-clipped ("S-02…", "510 lb" cut) with no fade mask or edge padding, making the scroll boundary look broken.
- **LOW** [dark] Unassigned stops filter segmented control (left panel): Inactive tab labels ("Urgent", "High+") in dark mode are dimmer relative to the pill background than in light mode — still legible, but contrast is noticeably lower than the light-mode equivalent.

> Pipeline notes: Capture produced 2 scroll segments per scheme (4 PNGs). Gemini call succeeded on first try, but 5 of its 8 findings did not survive pixel-level verification: it claimed the right-panel Hours bar had no fill and that light-mode Hours tracks were black (both wrong — the black segment IS the fill on a light-gray track), claimed grid lines bleed through the bottom helper text (they don't), claimed the column titles are misaligned (baselines match within ~1px in identical-offset crops), and claimed chip text padding is unbalanced (it is roughly centered). The most severe confirmed defect — grid lines drawn over the stop chips — was found during manual verification, not by gemini. Top-edge content clipping in the 2of2 shots is gallery page scroll, not a template defect, and was dropped.

## product-features-reviews (1 confirmed)

- **LOW** [both] reviews list - photo attachment chips: Attachment filename label truncates to 'switch-sw...' because the label is constrained to the ~55px thumbnail width; any filename longer than ~9 characters is cut off (Priya Sundaram review, 3of3 segments).

> Pipeline notes: Capture: first shoot.mjs run timed out on networkidle; retry succeeded (6 screenshots). Gemini returned 8 findings but 7 were false positives, verified by re-cropping regions at 2x-3x with a playwright-based crop helper: (1) green feature image does NOT bleed off the card edge - it shares the same right margin as the dropdown and text column; (2) attachment labels in light mode are dark-gray-on-white, not hardcoded dark; (4) dark-mode inactive segmented-control tabs are light gray on dark track, fully readable; (5-8) checkmark alignment, list indentation, column gutters, and vertical centering are all consistent across the three feature rows. Also self-checked: dark-mode headings/review titles that looked dark in the downscaled full-page reads are actually light-colored (downsampling artifact), and the dark-mode rating histogram, buttons, and pills are all correctly themed. Template is in very good shape overall.
