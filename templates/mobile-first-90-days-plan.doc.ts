// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Rampline — First 90 Days',
  description:
    "MOBILE (390px shell) onboarding ramp planner for Rampline, frozen at Day 34 of 90 (started May 18, 2026), where time is the primary control: the 52px navBar carries a 36px three-segment 30/60/90 PhaseDial (3×104° + 3×16° gaps = 360° exactly) whose outer task arcs fill per-phase completion (Land 6/8 = 75%, Integrate 2/6 ≈ 33%, Own 0/4 = 0%) over inner 1:1 relationship sub-arcs (7/10, 1/6, 0/2 — Marcus's all-missed 0% still shows its track), tap-advancing 30→60→90→30 with ArrowLeft/Right parity, a directional ±24px PhaseTransition slide, and 'Go to Day X' actionSheet rows as the non-gesture path. The default Day 60 view stacks a 72px phaseHeader ('Day 60 · Integrate' / 'Day 34 of 90 · 26 days left in this phase' / '2/6'), a 'SLIPPED FROM DAY 30 · 2' card of 72px amber-railed SlippedCarryRows ('Slipped from Day 30' chips, swipe-reveal Done at −72 / Re-home at −144 plus the mandatory 44×44 ellipsis menu: Mark done / Re-home to Day 60 / Send back to Day 30), six 60px Day-60 task rows (28px check circles in 44px hits, ≥3:1 CHECK_RING boundary per the amendment), and the 'All 8 tasks in this view' caption (2 + 6 = 8). Day 30 renders locked with slipped-away ghosts ('Now in Day 60' + View-in-Day-60 buttons); Day 90 derives six non-mutating ghost carries (4 open Day-60 natives + 2 slipped) each with a 'Re-home here' button. People holds three 72px stakeholder rows with 90px CadenceDotStrips (6×10px dots, held/missed/upcoming, caption '4 held · 1 missed' as the accessible text) feeding the dial's inner sub-arcs; Wins holds Day 30/60/90 filter chips (Day 90 = the true-empty TrophyIcon state with one 'View Day 60 wins' action) and a 5-win log ('5 logged · 3 in Day 30 · 2 in Day 60'). The signature chain: completing t7 'Meet your skip-level manager' in one write fills the dial 30-segment 75%→87.5% (7/8), flips Marcus's Day-60 dot upcoming→held (sub-arc 1/6→2/6, strip '1 held · 2 missed'), mints a suggested win (Wins badge '1', brand-tinted accept/dismiss card appending 'Day 60 · Week 5' → '6 logged · 3 in Day 30 · 3 in Day 60'), and moves the global stat 8/18 = 44% → 9/18 = 50% — every mutation executes immediately with a snapshot-exact Undo through the single sticky-in-flow toastDock (absolute bottom-76 only during sheet scroll-lock). Edit mode (actionSheet 'Edit tasks' only) flips the navBar to Cancel / '3 Selected' (aria-live) / Done and swaps the 64px tabBar for an editToolbar with Delete at the LEADING end and per-phase Move buttons (in-view phase disabled). Task/stakeholder detail live in two-detent bottom sheets (55% / calc(100%−56px), grabber-as-button, focus({preventScroll:true})); per-tab scrollTop and phaseInView persist in screenByTab with active-tab re-tap as the one legal reset; no skeletons ship (no refresh verb — fixtures render instantly). At desktop container widths (≥720px) it renders as a centered 430px phone column. Choose over mobile-parking-session when the object is a multi-week PLAN with phase navigation, slip provenance, and relationship cadence rather than a live metered session with money math; choose over mobile-moving-day-command when time is a navigable dial of phases with carry-forward accounting, not a single-day dependency race; choose over mobile-time-budget-digest when arcs encode task/relationship completion per phase rather than a minutes budget rationing content; choose over any desktop template when the brief calls for a phone-first 390px shell with tab bar, bottom sheets, swipe-reveal rows, and 44×44 touch targets.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
