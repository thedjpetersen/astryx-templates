// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Trellin — Application Pipeline',
  description:
    "MOBILE (390px shell) candidate-side job-application triage surface for Trellin: a 52px navBar (44×44 SlidersHorizontal filter sheet, 18px trellis-arrow mark + 'Trellin' over a LIVE '20 active · 22 total' tabular subtitle, 44×44 RefreshCw) above a sticky 56px FunnelStrip whose five 40px-tall count cells flex-grow PROPORTIONALLY to the live stage census (Applied 8 / Screen 6 / Onsite 4 / Offer 2 / Closed 2 = 22, radiogroup with arrow keys, active cell = brand fill with 8.5:1/8.9:1 contrast pairs), then five inset-grouped stage listCards of 88px appRows — each row wears a 44px StalenessRing (indigo→gray decay at 8%/day: a01 0d full indigo vs a13 21d fully gray, --color-error notch + '· going cold' meta at ≥7d) around a 36px monogram, two ellipsizing text lines (a13 'Meridian & Blackwood Talent Partners' / 'Principal Talent Research Partner, Executive Search' is the double-truncation stress), a 5-dot StageRail with 4px-halo pulsing current dot, ≥3:1 future-dot borders, and a downward --color-error rejection fork on a21, and a 44×44 ellipsis. The SIGNATURE MOVE is drag-with-consequences: pointer-dragging a rail's ghost dot to a new detent commits update(id,{stage}) through the ONE useReducer owner — the row FLIP-slides 240ms into its new stage card (fade under reduced motion), the FunnelStrip re-proportions live, the navBar subtitle re-derives ('19 active' on a close), tab badges recompute (Tasks 3 due-today, Insights = open-offer count), and a persistent no-timer Undo toast in a sticky bottom:76 dock restores the exact fixture-order position; the same verbs ship through the ellipsis action sheet (context header, destructive-last 'Move to Closed', Cancel card, focus-on-Cancel). Tasks tab: 28px large title, DUE TODAY 3 / THIS WEEK 4 / DONE 2 sections of 60px check-toggle rows (24px selection circles, whole-row checkbox semantics, undoable). Insights tab: OfferCompareCard (Northwind $156,000 vs Halyard $148,000, footer 'Δ $8,000 · Northwind leads' that re-derives on any move to Offer and collapses to a HandshakeIcon true-empty at zero), three ConversionBars (14 of 22 · 64%, 8 of 14 · 57%, 3 of 8 · 38% from cumulative maxStage), and a '5 going cold' list sorted 21/12/9/8/7d. Row tap opens a two-detent detail sheet (28px-dot rail, literal-date timeline, inline 36px stage-move buttons as the sheet's non-gesture path, 'Log touch' primary that re-tints the ring live). Per-tab filter + scrollTop persist across tab switches; re-tapping the active tab scrolls to top and clears the filter. At desktop container widths (≥720px) it renders as a centered 430px phone column. Choose over mobile-parking-session when the object is a MANY-ROW triaged collection with per-row stage state and a proportional aggregate strip, not one live session with a gauge and tiered pricing; choose over mobile-moving-day-command when rows move BETWEEN groups via a per-row drag rail rather than checking off a dependency graph against a deadline; choose over mobile-newsletter-triage when the mutation is stage progression with funnel analytics, not batched roll-up/unsubscribe of senders; choose over any desktop pipeline/kanban template when the brief needs a phone-first 390px shell with sticky funnel chrome, bottom sheets, and 44×44 touch targets.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
