// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Pitchin — Group Gift Pool',
  description:
    "MOBILE (390px shell) group-gift pool for Dana Whitmore's farewell Breville Bambino espresso machine ($248 gift + $12 shipping = $260 goal), organized by Maya Chen with '2d 14h left': a 52px navBar (three-coin bow-knot Pitchin mark, \"Dana's Gift\" title, 28px countdown deadline chip that flips to a green Funded chip at 100%), a 72px gift media row + 44px price utility row, then the PoolThermometer — '$237 of $260' 22px hero with a 91% brand figure, a 28px-tall track inside a 44px hit strip where six explicit rose-ramp segments (Maya 23.08% · Diego 19.23% · Priya 15.38% · Sam 13.46% · Lena 10.38% · Tom 9.62%, sum 91.15 = 237/260) are real tappable buttons that highlight and scroll to their ledger row — a share-message card whose copy re-derives from remaining ('only $23 to go!' → '$13' after a $10 pledge), and a role-aware 11-row ContributorLedger (six pledges, two chat messages including Priya's 100-char single-line-ellipsis stress, a 'Maya nudged Ana' event, and two invited-pending rows with 36px Nudge pills — Ana Silva-Konstantopoulos is the 320px trailing-control truncation stress). The signature move is the sticky pledgeBar CTA ('Chip in — $23 to go') opening a 55%-detent sheet with a horizontal PledgeDial ruler: $1 ticks at 12px spacing from $5–$100, pointer scrub with momentum decay, a magnetic 'Covers the gap' detent flag at $23 (the $23 tick drawn 2×24 in brand), a full role='slider' keyboard map (arrows ±1, PageUp/Down ±5, Home=$5, End=$100) plus $10/$25/'Cover the gap · $23' preset pills — and every write to the ONE dialAmount re-derives the dashed provisional thermometer segment above the sheet, the 3% fee readout ('+ $0.69 fee · $23.69 total'), the share copy, and the sheet CTA in the same render. Crossing 100% flips the surface from collecting to ordering: Funded chip, 240ms completion sweep (removed under reduced motion), the ACTIVITY header's 'Unlocks at 100%' lock swapping to a 44×44 organizer menu (actionSheet: 'Close pool & order' executes immediately with an Undo toast per undo-over-confirm, 'Message contributors', destructive 'Refund all pledges' routing through a centered alertdialog whose scrim does not dismiss), and the CTA morphing to 'Close pool & order'. Overshoot is derived, not hardcoded: a $25 pledge shows '101%' with the track capped and 'Funded · $2 over'. At desktop container widths (≥720px) it renders as a centered 430px phone column with hairline borders. Choose over mobile-parking-session when the surface is social money-pooling toward a shared goal (segmented per-person progress, nudges, organizer unlock) rather than a live metered session with a countdown gauge and tiered pricing; choose over mobile-meal-quickadd-log when the one live value is a currency dial with a magnetic snap point feeding a group ledger, not repeated quick-add taps against personal macro budgets; choose over mobile-banking-home-insights when the money story is one collective goal crossing 100% and changing the surface's role, not account-wide spend filtering and privacy masking; choose over any desktop template when the brief calls for a phone-first 390px shell with sticky navBar/pledgeBar, bottom sheets, an actionSheet + alert chain, and 44×44 touch targets.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
