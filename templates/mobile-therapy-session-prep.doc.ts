// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Tether — Session Prep',
  description:
    "MOBILE (390px shell) therapy session-prep assembler for Tether, frozen at Tue Jul 7 (next session Thu Jul 9 · 4:30 PM with Dr. Elena Ruiz → the fixed 'In 2 days · 3 ready' CountdownPill): a 52px navBar (KnotMark scroll-to-top, derived countdown pill, 44×44 History) over the 28/700 'Session prep' large title, a 220px MoodWeekTimeline card — 7 fixed 46px day columns (334px rail, exact fit at 390, snap-scrolls with a Sunday peek below 374px) over a 160px 5-band valence plot with a midline hairline, exactly 17 feeling-tagged 14px dots (per-day 3+2+4+1+3+2+2; anxious 5 + calm 5 + drained 3 + frustrated 2 + proud 2) in 46×44 greedy-stacked buttons, 2 brand-halo rings marking the moment-sourced talking points, caption '8 of 17 moments steady or better · week trend −0.2' deriving live from the rows — then a 3×76px TalkingPointStack (rank numerals, 20px OriginChips 'Wed · anxious' / 'Jun 27 session', amber 'Carried over' badge, 44×44 ellipsis menus with Mark discussed / Remove point), a 2×60px FROM LAST SESSION card (Boundary-setting 'In your list' + pending 'Sleep routine follow-up' Add), and a sticky 64px footer (5-segment DensityMeter mini '3 of 5' + 'Open prep card'). The signature move is provenance-aware drag-to-assemble: pointerdown+8px on any un-haloed dot lifts a DragGhostChip that rides the pointer while the stack opens a 76px dashed drop slot at the computed insertion index and the footer meter previews pointCount+1 mid-drag; dropping inserts the point, halos the source dot, bumps pill/header/meter to 4 in one dispatch, and announces through the single sticky toast dock (tap any dot for the mandatory action-sheet button path: context header, Add/Remove, View note, Cancel-first focus). The two-detent prep sheet (55% / calc(100%−56px), clickable grabber) live re-typesets a shareable PrepCardPreview that auto-densifies past 4 points; the 6th topic renders an error meter segment and flips the caption to 'Over budget — sessions fit 3–5 topics'. Edit mode flips the navBar to Cancel/Reorder points/Done, swaps the footer for an identical-geometry editToolbar (destructive Remove leading, Move to top trailing, disabled until a row is selected), and gives every row MinusCircle + up/down chevrons; removals and Mark discussed execute immediately with snapshot Undo restoring exact index and halo (no auto-dismiss timers). Deviations from spec: the long-text stress point lives as the Sat 4:50 PM anxious moment's note (the spec's 17-moment aggregates are law, so the stress text merged into the Mom-call moment); the same-band dot-collision stagger ships as deterministic layout logic though the 17 shipped moments contain no colliding pair; toolbar Remove/Move-to-top act on a tap-selected row since reorder mode has no multi-select. At desktop container widths (≥720px) it renders as a centered 430px phone column with hairline borders. Choose over mobile-parking-session when the surface assembles a document from logged evidence (drag dots → ordered stack → live-typeset card) rather than extending a metered countdown; choose over mobile-voice-memo-triage when raw material is a week of mood moments feeding a budgeted agenda with carry-over lineage, not audio spans minted into chips; choose over mobile-kid-reading-log when the payoff is a provenance-aware prep artifact under a 3–5 topic budget, not streak/goal gamification; choose over any desktop template when the brief calls for a phone-first 390px shell with sticky chrome, bottom sheets, an action sheet, and 44×44 touch targets.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
