// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Lintel — Doorbell Event Reel',
  description:
    "MOBILE (390px shell) door-camera triage surface for Lintel's Front Door camera on the demo's frozen 'Fri, Jul 4': a 52px navBar (Lintel door-arch/keyhole mark, compact title fading in via an IntersectionObserver sentinel, 44×44 Refresh) over a 52px 'Front Door' large title, then a 160×160 24-segment activity ring (13° arcs + 2° gaps = 15° × 24 = 360°, r 66, strokeWidth 14) whose 6 active hours color by visitor mix — amber for any-unknown (hours 6, 12, 17), indigo for known-safe (8, 11, 15) — with a 22px tabular '12 events today' center readout, a snap rail of populated-hour radio chips ('6–7 AM · 3', the ring's tap/keyboard path — segment overlays were dropped because hours 11/12 sit 15° apart, a 17px chord at r66 that cannot host clearanced 44×44 hits), a 36px filter-pill radiogroup (All 12 · Unknown 6 with '3 visitors' sublabel · Known 4 · Motion 2), and a day-grouped list (MORNING 6 / AFTERNOON 4 / EVENING 2) of 72px media rows — 48px deterministic porch-silhouette SVG thumbs (no photos anywhere: seed math x = 24 + ((N×37)%60) + k×28), two-line stack with ev-6's 'Person at front door — lingered' truncation stress, trailing 20px VisitorIdentityChip. Tapping a row expands an inline 8-frame filmstrip player: 120px stage (viewBox 0 0 320 120), 40×28 thumb snap rail with a pointer-draggable 48×36 loupe (role=slider, ArrowLeft/Right, snaps to 8 slots), 44×44 steppers disabled at frames 0/7, and a 2-second-step tabular timestamp ('6:12:06 AM' at frame 3; '12:38:14 PM' at ev-8 frame 7). The signature move is label-with-consequences: 'Label visitor' opens a two-card action sheet (focus to Cancel, preventScroll, Tab trapped) whose preset rows write ONE visitorLabels map entry — u1's 3 chips and 3 silhouettes recolor to the next palette accent, ring segment 6 flips amber→indigo, Unknown drops 6→3 events · 3→2 visitors, Known climbs 4→7 (7+3+2=12 holds) — held by a persistent Undo toast whose snapshot restores the exact prior maps (no timers; a second label replaces the first, stress-proving snapshot semantics); 'Not a person (reclassify)' flips that one event to Motion. Refresh shows 4 deterministic 72px skeleton rows (60/45/70/60 primary bars) resolved by the next filter tap; labeling all three unknowns yields the all-indigo ring and a filtered-empty 'No unknown visitors' state. A sticky-in-flow toast dock (bottom 68, absolute bottom 76 only while scroll-locked), 64px 4-tab bar with per-tab scroll persistence and a Clips badge fed by 'Save clip', and stub Cameras/Clips/Settings screens (working person-alerts switch with a ≥3:1 OFF track) complete the shell; at ≥720px container width it renders as a centered 430px phone column. Choose over mobile-banking-home-insights when the hero ring filters a MEDIA-EVENT day list with inline players rather than money categories over a ledger; choose over mobile-camera-roll-organizer when triage means identity-labeling recurring visitors across events (one map recoloring chips, silhouettes, and ring in one pass), not batch-selecting photos; choose over mobile-parking-session when the signature mutation is an action-sheet label broadcast with Undo, not a bottom-sheet extension with tiered pricing; choose over any desktop template when the brief needs a phone-first 390px shell with sticky navBar/tabBar, an action sheet, and 44×44 touch targets.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
