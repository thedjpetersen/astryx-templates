// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Foyer — DM Request Triage',
  description:
    "MOBILE (390px shell) DM waiting room for Foyer, run like an inbox-zero client: a 52px navBar (28px two-panel door-mark, sentinel-faded title, Refresh + Edit) over a 'Requests' large title and a 52px segmented filterBar whose 'People 8 · Brands 4 · Filtered 3' counts derive from one 15-record requestsQueue (8+4+3=15; the tab badge reads people 8 + brands 4 = 12 — filtered spam is quarantined out by design, and Settings › Filter spam OFF folds it back in, 12→15). The People card is 8 72px TrustSignalRows (8×72 + 7 dividers = 583px): 40px id-gradient avatar (circle person / 12px-radius brand), 16px/500 name beside an 11px mutuals pill ('0 mutuals' renders honestly for Jonas Berg) and a ≥3:1 account-age dot duplicated by 'Joined 2019' text, over a first line BLURRED at 6px until peeked (Marcus Webb ships peeked so blurred, unblurred, and the 6px peeked-dot all appear at rest). Row tap opens the 55%-detent ContextPeekSheet — 'Peeking is private — Priya won't know you looked.', full unblurred message, shared-groups/mutuals-avatars/joined rows, Decline|16px|Accept footer — and every close path (X, scrim, Escape, >120px grabber drag) marks peeked:true and restores focus. Swipe right reveals a 72px Accept block, swipe left a 144px Decline+Limit pair, with a 44×44 ellipsis menu (Accept/Decline/Limit/Block) and 450ms long-press as the mandatory button paths; Block alone routes through a centered alertdialog whose scrim does not dismiss. NavBar Edit swaps the tabBar for a 64px BatchSweepBar (Decline 5 leading, 'All in filter (8)', Accept 3 trailing pill) fed by per-row stance pills — fixture stances Priya/Dana/Amara accept (3) + five decline (5) = 8 ✓ — and executing sweeps all 8: badge 12→4, People goes true-empty while the badge still reads 4 (badge ≠ visible list), Chats 6→9, one persistent 'Accepted 3 · Declined 5' Undo toast that restores 12/8/6 wholesale. Accepting Priya alone animates her row out, drops the badge 12→11, and prepends a NEW-overline 'Moved to Chats' entry the Chats tab shows without resetting (per-tab scroll + segment persist; re-tap scrolls to top). Refresh renders 4 deterministic skeleton rows (60/45/70/60 primary bars) resolving on the next click to 'Updated just now'; Settings switches prove the single owner — Blur previews OFF unblurs every unpeeked row at once. Choose over mobile-parking-session when the surface is a list-triage inbox with batch edit mode, trust-signal rows, and quarantine math rather than a single live session with a gauge and tiered pricing; choose over mobile-camera-roll-organizer when selection sweeps carry per-item accept/decline stances and a privacy-peek sheet rather than photo-grid organization; choose over mobile-banking-home-insights when the one state owner drives triage verbs (accept/decline/limit/block) across badge, list, and chats surfaces rather than filter-driven spend analytics; choose over any desktop template when the brief needs a phone-first 390px shell with sticky navBar/tabBar, bottom sheets, swipe-reveal rows, and 44×44 touch targets.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
