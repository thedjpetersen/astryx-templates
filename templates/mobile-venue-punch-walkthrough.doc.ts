// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Snagline — Venue Punch Walkthrough',
  description:
    "MOBILE (390px shell) linear venue-walkthrough punch list for Snagline event ops at Riverside Hall (Meridian Product Summit — load-in Jul 12, inspector Dana Okafor), with NO tabBar — the walkthrough moves via Prev/Next. A 52px navBar (clipboard-with-map-pin brand mark · a 184px six-dot RoomDots radiogroup rail with arrow keys, amber dots for rooms with open items and one green dot for the clear Green Room · 44×44 CheckSquare sign-off) sits over a sticky 260px ZonePlan — a 358×228 skewX(−8) isometric SVG floorplan whose six rooms (Lobby/Main Hall/Stage/Bar/Green Room/Patio, id-derived brand-mix fills 14/20/8/17/11/14%) are real focusable buttons carrying live open-count chips and numbered severity-ring pins (2px cosmetic / 4px functional / 6px error-red blocker, 44px hit circles) — over a 56px Prev/Next roomNav and a per-room list: 72px open rows (blockers-first sort, swipe-right +72px to a brand Resolve block plus the mandatory 44×44 ellipsis menu with Resolve/Edit/Reassign zone/Delete), 60px struck resolved rows, 44px collapsed jump rows for the other five rooms, and the Green Room's 'Room clear' zero-state over its 2 struck rows. Fixture arithmetic is locked: open by zone 2+4+3+2+0+1 = 12, resolved 4, 12+4 = 16 total; the 12 open split blocker 2 + functional 5 + cosmetic 5 and venue 7 + tape 5, which the 64px sticky HandoffLedger renders live as 'VENUE FIXES 7 | WE BRING TAPE 5' beside an always-enabled '12 open' pill that jumps to the first open room and nags via toast until zero, then flips to a brand 'Sign off' opening the z50 cover + 'Sign off walkthrough?' alertdialog. The signature move is file-with-consequences: tapping a zone (or its 'Add item' header button) opens the two-detent sheet (55% / calc(100%−56px), zone pre-tagged with an in-sheet 6-chip picker, blur-validated title, severity + responsibility segmented controls) while a dashed ghost pin marks the drop point; 'File item' commits ONE state write that appends the item, re-sorts the list, pulses the linked pin + row for 1200ms (static highlight under reduced motion), increments the room chip, recomputes the ledger split, and announces through the single sticky toastDock — while Resolve/Delete execute immediately with a persistent snapshot-exact Undo (resolving Patio's last item dims the room to a resolved tint, flips its dot green, and Undo reverts all five surfaces atomically). Desktop container widths (≥720px) render a centered 430px phone column, never a stretched relayout. Choose over mobile-site-photo-checklist when the object is a room-by-room DEFECT LIST pinned to a floorplan with a two-party handoff ledger, not a photo-evidence capture flow; choose over mobile-moving-day-command when the walkthrough is linear inspection of one venue (plan + punch rows + sign-off lock), not multi-crew day-of logistics; choose over mobile-parking-spot-memory when the SVG map is a six-zone workspace whose regions are live counters and pin canvases, not a one-pin recall aid; choose over mobile-weekly-review-walkthrough when 'walkthrough' means a physical space with spatial pins, not a GTD review of lists.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
