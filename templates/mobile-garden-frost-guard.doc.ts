// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Coldframe — Garden Frost Guard',
  description:
    "MOBILE (390px shell) frost-triage surface for Coldframe, a home-garden frost guard over deterministic bed-and-forecast fixtures: a 52px sticky navBar (28px sprout-in-snowflake SVG mark, 'Coldframe' compact title, 44×44 Sunrise checklist + RefreshCw buttons) over a 44px riskSummaryBar that reads '2 beds at risk tonight · low 29°F' on an amber tint (≈6.2:1/7.1:1 pairs) and flips to a green 'All beds protected' band as covers land; a 232px OvernightLowCurve card (44px '6 PM – 8 AM · 4 hrs below 32°' header + 180px SVG: 15 hourly temps 46→29→37°F, dashed 32° frost line with a 12% brand-tinted below-frost area, and one aria-hidden marker per at-risk/watch bed at the 4 AM minimum that translates up 24px — +4°F — when its bed is covered, Herb Spiral's WATCH marker sitting exactly ON the frost line); an AT RISK coverQueue keyed by ORIGINAL risk (rows never jump cards mid-undo-window) with a 48px 'Cover all 2 at-risk beds' bulk row (replaced by a caption when exhausted — dead buttons banned) and two 96px BedRiskThermo rows — 44px-wide dual-threshold vertical thermometers (8px track mapping 24–44°F at 3.2px/°F, ≥3:1 rest track, floor tick + 11px label) whose orange danger-overlap segment (tomato: 33−30 = 3° = 9.6px) collapses via a 200ms height transition when covering lifts the effective low past the floor — each with swipe-to-cover (release past −36px commits immediately, undo-over-confirm) AND a visible 44px-hit Cover/Uncover button; a SAFE TONIGHT card of 60px rows with SAFE/WATCH/COVERED pills; Beds tab (52px largeTitle, '6 beds · 39 plants' cross-check, six 72px media rows with id-derived gradient swatches); History tab ('4 frost nights · 11 covers this season' both derived — Jun 21's 33° row renders without the Snowflake glyph); and a MEDIUM/LARGE-detent morning-checklist sheet whose task list is LIVE-derived from the same bedProtection map ('Tomorrow · 2 tasks' → '4 tasks' as 'Uncover Tomato Starts after 9 AM' rows appear). Every mutation flows through ONE state owner: covering a bed moves the thermo, pill, curve marker, risk band, Tonight tab badge, and sheet in one patch, announced by a persistent no-timer Undo toast in a sticky-in-flow dock (bottom 76) — bulk cover reverts BOTH beds from one snapshot. RefreshCw swaps both bed cards to exact-height 96/60px skeleton rows resolving next frame. At desktop container widths (≥720px) it renders as a centered 430px phone column. Choose over mobile-parking-session when the object is a multi-entity triage queue (six beds, dual thresholds, bulk action) rather than one live metered session with money math; choose over mobile-baby-care-log when the surface forecasts and mitigates a coming overnight event (cover now, checklist tomorrow) rather than logging a stream of past care events; choose over mobile-site-photo-checklist when completion means flipping protection state with instant cross-surface consequences, not capturing evidence per slot; choose over any desktop weather/dashboard template when the brief needs a phone-first 390px shell with sticky chrome, bottom sheet, swipe rows, and 44×44 touch targets.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
