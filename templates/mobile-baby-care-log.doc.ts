// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Moonpip — Baby Care Log',
  description:
    "MOBILE (390px shell) newborn care log for Moonpip, frozen at 1:58 PM Thu Jun 25, 2026: a 52px navBar (crescent-and-pip mark, 'Juniper · 11w 0d' identity h1 button opening a profile sheet — born Apr 9, 2026 = exactly 77 days — and a 44×44 RefreshCw) over four tabs. LOG is the 3am screen: a 44px NextFeedBanner computed from feed-start intervals (155+165+160+160 = 640 → avg 160 m → 'Next feed was due ~1:55 PM · 3m ago' in error tint), a 36px statChipRow (Feeds 5 · Nursing 69m · Sleep 6h 50m, every number derived live from the rows), and the DualRailDayline — a 1,104px 24-hour canvas (44px/hour + 24px pads) with nursing/bottle pills right-aligned to a 40px labeled time spine on the left rail, duration-proportional sleep blocks (105 m → 77px) on the right, a 45° hatched gap-alert band spanning both rails 1:55→1:58 PM (itself a 44px button opening Quick log), and a 2px brand NOW rule — over a sticky actionDock ('Start feed' with 'Last ended L → start R' side memory + 'Quick log'). The signature move is the two-channel SideToggleTimer: starting a feed docks a 56px strip under the navBar that persists across ALL FOUR TABS, its L/R halves are real buttons that swap accumulation mid-feed (frozen '4:10' vs live), and 'End' executes immediately (undo-over-confirm) cascading ONE store write into the new 1:58 PM pill, the cleared gap band, the recomputed prediction (intervals +163 = 803 → avg 160.6 → 'Next feed likely ~4:40 PM'), chips 5→6, Trends' hatched today bar, a prepended Care-team HandoffLine, and a persistent 'Feed logged · Undo' toast whose Undo reverses all six surfaces in one snapshot restore. TRENDS: 28/700 large title with IntersectionObserver compact-title fade, segmented Feeds/Sleep/Bottles — 7-bar charts with dashed avg lines (7.5 feeds; 850 m = 14h 10m) and the arithmetically locked bottle card (12 bottles · 1,140 ml · avg 95 ml). CARE TEAM: full-bleed 72px handoff stream of today's 10 events attributed to Maya/Dan/Rosa (night doula), 4-row deterministic skeleton via the refresh press, UserPlus invite sheet with on-blur email validation. SETTINGS: ml/oz units segmented (30 ml = 1 oz), a reminder switch with the ≥3:1 OFF-track literal that hides the gap band, an expected-interval stepper (2h 40m, ±5 m, spinbutton), and sign-out via ellipsis → action sheet → centered alert that resets the fixtures. Quick log ships the GloveStepper — a 240×64 one-thumb ml stepper with full-half taps, 34px readout, and 30/60/90 SET pips. At desktop container widths (≥720px) it renders as a centered 430px phone column, never a stretched relayout. Choose over mobile-parking-session when the object is a two-sided LIVE timer feeding a dual-rail day timeline and a multi-caregiver handoff feed, not a single countdown gauge with tiered pricing; choose over mobile-sleep-morning-report when you need same-day logging verbs (timer, quick-log sheet, per-entry edit/delete with Undo) rather than a next-morning readout; choose over mobile-meal-quickadd-log when events are duration-shaped (nursing sides, sleep spans) on a 24 h spine instead of discrete meal entries; choose over any desktop template when the brief calls for a phone-first 390px shell with sticky chrome, bottom sheets, and 44×44 touch targets.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
