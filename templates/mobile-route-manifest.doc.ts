// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Parceline — Route Manifest',
  description:
    "MOBILE (390px shell) courier route manifest for Parceline's Route 12 (Maya Reyes, Van 2214, shift start 8:40 AM): a 52px navBar carries the route-progress fraction '7/23 stops' and an ETA-drift pill ('+8 min' on the 5.9:1 red-tint pair; early/on-time flips to the green-tint pair) whose 44×44 hit jumps to the Shift tab. The Route tab pins an 'Up next' heroCard (sticky top:52 — 'UP NEXT — STOP 8 OF 23', 2205 Harrow Ln 22px/700 2-line clamp, D. Okafor · 3 packages, Window 10:45–11:15 AM + brand 'ETA 10:53', a 48px Delivered primary and 44×44 ellipsis) above a 64px ManifestScrubber (role=slider SVG route polyline, 23 nodes at 20px pitch, Catmull-Rom segments brand-stroked through the attempted count, ringed up-next node, drag scrubs with a floating HUD chip while 44×44 prev/next chevrons + arrow keys are the button path) and a full-bleed StopRail: 60px stop rows with a 40px spine zone drawing the 12px status node (pending hollow at the 3.2:1 amendment border, delivered brand+check, failed error+'!'), 1–3 package pips, a ±30° drift tick (early leans left — stops 2/6/8/12/16/19/22), and the 2px connecting line; pending rows swipe RIGHT to a +72px snap revealing a LEFT brand Delivered block with the mandatory ellipsis fallback, and the completed section (COMPLETED — 7) collapses behind a 44px disclosure row. Fixtures cross-check by law: 23 stops, package counts 12 (stops 1–7) + 29 (stops 8–23) = 41; delivered 10 + exception 2 + pending 29 = 41; drift = attempted deltas = +8 min; windows generated as 9:00 AM + (n−1)×15. The signature move is one-commit re-derivation through the single route store: delivering stop 8 moves the fraction 7/23→8/23, drift pill +8→+5 (8 + stop 8's −3), rail fill 7→8 segments, hero promotion to stop 9 (ETA 11:05), scrubber aria-valuenow, section counts, and Shift statGrid together, with exact-snapshot Undo via the sticky toast dock (+8→+5→+8→+5 round-trip proves derived-not-cached); 'Report attempt failed' (destructive LAST row of the centered-no-icons stopActionSheet — reversible, so NO confirm dialog) routes the stop into Exceptions whose Retry re-queues it as the new first pending, and the large-detent proofSheet (stylized SVG porch, id-derived 'SIMULATED CAPTURE' gradient at hue (stopId×47)%360 with a deterministic H+180 Retake, pre-inked signature with Clear/Re-ink, three 44px whole-row switches) confirms through the same deliver action. Scan tab: 36px searchField over 72px package rows (PL-1296-A/B/C for stop 8) with per-row Scan→Scanned flips and a filtered-empty echoing the query ('PL-9' matches nothing). At desktop container widths (≥720px) it renders as a centered 430px phone column. Choose over mobile-parking-session when the object is a WORKING ROUTE QUEUE — a sequenced list the user burns down with per-item verbs, spine/progress glyphs, and an exceptions lane — not a single live session with a gauge and extend/end economics; choose over mobile-transit-companion when the user OPERATES the route (delivered/failed/retry mutate ordering and drift) rather than rides it; choose over mobile-luggage-scan-trail when scanning is a side duty of a stop-by-stop manifest (packages grouped per stop, drift math in the chrome) rather than the surface's whole spine; choose over any desktop template when the brief needs the 390px shell, 4-tab bar, swipe-reveal rows, bottom sheets, and 44×44 touch targets.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
