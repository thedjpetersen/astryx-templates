// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Driftwood — Someday/Maybe Sorter',
  description:
    "MOBILE (390px shell) someday/maybe triage surface for Driftwood, frozen at TODAY = Jul 4 over 52 deterministic ideas (12 queue + 32 shelved + 5 released + 3 activated, every capturedAt/ageDays/staleDate triple hand-proven at staleDate = capturedAt + 90d). Sort tab: 52px navBar (24px paper-boat mark whose stern curls into a return arrow, 'This week · 3' pill that jumps to the Resurfacing tab's Activated section) over a ~176px focusCard — 'Build a backyard cold frame from the old windows', 37 days old, 22/700 title, 8px RipenessBand (fixed 4-stop scale: Fresh #34D399 0–14d → Ripening #FBBF24 15–45d → Overripe #F97316 46–90d → Compost #78716C >90d) with a 2px marker at 41.1% and the 'goes stale Aug 26' caption carrying the accessible encoding — then the signature 3-dock verdict row: Activate (brand fill), Shelve (muted), Release (1.5px amber caution boundary, NOT error — it's recoverable), each a real 44px button while dragging the card at a dock scales it 1.08 and swaps its label for a consequence preview ('This week: 3 → 4' / 'Adds to shelf (32)' / 'Recoverable from Resurfacing'). Below: an 11-row UP NEXT listCard (60px rows, 4×44 ripeness slivers, 44×44 ellipsis menus with Move to top / Shelve… / Release; the 94-day 'Screenplay about the ferry line' reads 'went stale · 4 days ago' in amber; a 91-char garage-conversion title exercises 320px ellipsis) and the 'All 12 in queue' terminal caption. Shelve opens the CadenceDialSheet — a 55%-detent sheet whose body is a scroll-snap wheel of five EVENT-RELATIVE anchors ('Next weekly review — Sun', 'After Kitchen reno ships', 'First frost — Oct', 'Spring thaw — Mar', 'When queue drops under 8'), never raw dates, each showing its live '· n on shelf' load; only Done commits (X/scrim/Escape cancel with no verdict). Shelf tab: 'On the shelf · 32 ideas across 4 cadences' over a 232px ShelfMap of four sagging SVG planks (sag = min(max(load−4,0),10)px → 5/9/2/0; 'After Kitchen reno ships' at 13/12 wears a '+1 OVER' strain badge with 4°-rotated end ticks; shelving there proves the 10px sag cap at 14) whose book-spine ticks tint by ripeness, then four bucket cards with exact loadMoreRows (Show 6/10/3/1 more). Resurfacing tab: Activated (3), Released — recoverable (5, per-row Restore returns ideas to the queue tail; restoring all five reveals the WindIcon empty state and 'All 17 in queue'), and Next resurfacings utility rows. Release executes immediately with a PERSISTENT Undo toast (no timer) that restores the exact queue index; one sticky-in-flow aria-live dock announces every mutation; per-tab scroll and expansion survive switches; re-tapping the active tab scrolls to top. Desktop container widths (≥720px) render a centered 430px phone column. Choose over mobile-parking-session when the object is a triage QUEUE with verdict docks and capacity visualization, not a live metered session with a countdown gauge; choose over mobile-story-planner when the one ruling array is a someday queue ripening toward staleness rather than a reorderable filmstrip with a duration budget; choose over mobile-banking-home-insights when the signature dispatch is a three-verdict sort (activate/shelve/release) rather than a category filter threading a spend ring and ledger; choose over any desktop template when the brief calls for a phone-first 390px shell with sticky navBar/tabBar, a bottom sheet, anchored row menus, and 44×44 touch targets.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
