// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Astryx — Overlay & Feedback Gallery',
  description:
    "MOBILE (390px shell) specimen sheet documenting the kit's overlay and feedback contracts in the composer-state-gallery tradition: nine sections under sticky blur GallerySectionHeaders (top 52, z15) inside a 52px navBar shell with NO tabBar, so the one real polite toastDock sits at bottom 16 (its annotation cites the bottom-76-above-a-64px-tabBar variant). S1 freezes an OPEN action sheet inside a clipped SpecimenStage — z40 scrim, insetInline-16/bottom-16 card pair, two-line context header, four 56px 17/500 rows with 'Remove from Inbox' last in --color-error, separate 56px Cancel card — beside the memorized decision table (scrolls→sheet · blocking irreversible→alert · 2–5 flat verbs→actionSheet) and REJECT tags for 6+ verbs and confirm-for-undoable. S2 is a LIVE anchored menu: 60px highlight row whose 44×44 ellipsis is the mandatory button path (450ms long-press is garnish), opening a 236px-wide z41 card of four 44px rows over an in-stage scrim, with focus trapped, restored to the trigger on every close path, and narrated by a FocusReadout; Delete Highlight dims the row and posts an undo toast. S3 proves undo-over-confirm live — archiving any of three 60px thread rows executes immediately and posts to the REAL dock; Undo restores the exact original index and the toast flips to 'Restored'; a second archive REPLACES the toast (the one-toast law), and the frozen trio + overlapped-toasts REJECT document the 48px/radius-12 anatomy and the no-auto-dismiss-timers law. S4 freezes the centered alert (min(280px, calc(100% − 64px)), z60/61 above covers, verb buttons split by a hairline, scrim-click-does-NOT-dismiss). S5's segmented radiogroup really switches one banner across info/success/warning/error color-mix tints; S6 seats countBadges (3, 12, 127→'99+'), a dotBadge, and Synced/Pending/Failed statusPills; S7 shares one pct between a 48px SVG ring and a 4px bar (Advance +20% disables at 100; CSS-only indeterminate sweep that reduced-motion removes entirely); S8 draws the ten-rung z ladder (0/10/15/20/30/40/41/50/60/61) as stepped brand-tinted plates; S9 pairs the focus-restore matrix with a live 'Copy link' proof that toasts never steal focus. Frozen specimens are aria-hidden pictures with pointerEvents none and no dialog roles; every annotation is real 11px text; the navBar theme toggle flips colorScheme so both themes render by construction. At desktop container widths (≥720px) the shell centers as a 390px hairline column on a muted canvas. Choose over composer-state-gallery when the anatomy being taught is the MOBILE overlay/feedback kit (action sheets, undo toasts, alerts, scrim layering) rather than a desktop chat composer's states; choose over bottom-sheet-snap-explorer when you need the whole overlay vocabulary as citable frozen contracts, not one live sheet's drag mechanics; choose over mobile-parking-session or any product-shaped mobile template when the deliverable is the reference sheet reviewers grep — labeled specimens with exact px annotations — not a working app surface.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
