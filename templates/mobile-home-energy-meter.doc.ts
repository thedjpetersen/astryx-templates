// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Wattling — Home Energy Meter',
  description:
    "MOBILE (390px shell) whole-home power meter for Wattling — a deterministic playback instrument, not a dashboard poster. One MeterClockStore owns time: mode 'live'|'pinned' plus a cursor tick 0–18 over a scripted 4:00–7:00 PM window (10-minute steps, live tick 18 = 7:00 PM, no ambient timers). On screen: a 52px navBar (28px sparrow mark whose wing is a pulse-waveform stroke, 'Wattling' over a state-driven 'Live · 7:00 PM' subtitle, and a LivePill that is both mode indicator and persistent return-to-live button); a 240° dial gauge (viewBox 260×200, 0–18 kW, needle eased 400ms, decaying peak-hold ghost at 16.44 kW · 6:00 PM, 44px tabular '11.28 kW' readout); five FLIP-ranked 60px ApplianceStreamBars (Heat Pump 3,150 · Dryer idle · EV 7,200 · Refrigerator 150 · Everything-else 780 at live; bars fraction-of-7,200 with 2px 'Idle' stubs); a 19-point TimeScrubStrip whose draggable playhead IS a role='slider' (ArrowLeft/Right one tick, Home → 4:00 PM, End → live); and a CostTicker deriving $21.17 from KWH_CUM 68.28 × $0.31. The signature move is one-scrub lockstep: pin 5:50 PM and the needle snaps to 16.44 kW, EV 7,200 overtakes the Dryer 5,400 in a 300ms FLIP (t10→t11 is keyboard-reachable in two keystrokes), the ticker freezes at $16.55 'as of 5:50 PM', and the navBar subtitle rewrites — then 'Return to Live' plays a deterministic 60 ms/tick catch-up sweep (dryer shutoff, EV ramp, 3 HVAC cycles in one pass; reduced motion jumps instantly and the toast alone narrates). The non-gesture path is a two-detent (55% / calc(100%−56px)) 'Jump to time' sheet with a snap-scroll list of 19 flat 44px rows and a 44px center highlight band — every row directly tappable, no OS drum-roll clone — whose 48px footer label tracks the selection ('Pin to 4:00 PM'). Usage/Devices/Settings tabs are real lightweight screens with per-tab state persistence and on-screen reconciliation: Devices' 'Metered today 52.28 kWh' plus the printed 'plus 16.00 kWh before 4:00 PM = 68.28 kWh' ties to the ticker, and Usage's 'Today · so far' row tracks the cursor live. Dial track, bar tracks, and the switch OFF track are meaningful rest fills at ≥3:1 vs the card (explicit light-dark pairs with math comments); the quarantined brand pair is #4C7A0B/#A9E34B (raw #5C940D fails 4.5:1 on white and never renders). At desktop container widths (≥720px) it renders as a centered 430px phone column, never a stretched relayout. Choose over mobile-parking-session when the object is time-scrubbed PLAYBACK of a recorded signal (snapshot-and-catch-up, rank FLIPs, frozen aggregates) rather than a live countdown session with extend/end verbs and tiered pricing; choose over grid-feeder-console when the brief is a phone-first consumer meter with a tab bar, bottom sheet, and 44×44 touch targets, not a desktop operations console; choose over mobile-banking-home-insights when the scrubbed dimension is TIME against one physical signal, not a category filter across a transaction ledger; choose over mobile-run-recap when the cursor scrubs a live utility feed you can return to, not a finished workout's distance axis.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
