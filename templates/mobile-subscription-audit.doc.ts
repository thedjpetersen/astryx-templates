// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Prune — Subscription Audit',
  description:
    "MOBILE (390px shell) subscription-triage bench for Prune, frozen at Mon, Jul 6 2026: a 52px navBar (snipped-loop 'p' mark, IntersectionObserver fade-in title, 44×44 RefreshCw driving a deterministic 4-row 72px skeleton → 'Updated just now' cycle) over a 28px 'Audit' large title, a PINNED 56px SavingsMeterOdometer (sticky top:52 z19, per-character translateY-rolling 22px tabular digits opening at $13.98/mo · $167.76/yr, minWidth-reserved so the $455.76 digit-count jump never shifts layout), a 96px RenewalHorizonStrip (60 scroll-snap 44px day columns Jul 6–Sep 3, price bubbles tiered 28/32/36/40/44px with 1px brand borders for the ≥3:1 boundary law, the Jul 10 three-bubble collision stack captioned $27.98, every bubble a Tab-reachable button opening that sub's sheet, ArrowLeft/Right scrolling the focused rail), and REVIEW·6 / KEEP·4 / RECLAIMED·2 sections of 72px VerdictRailRows (44px monogram tile, two-line stack, green DeltaChip, 44×44 Decide ellipsis). The signature move is the 3-zone verdict rail: dragging a row right +72px reveals the brand Keep block, left −72px the amber Pause block deepening to the error Cancel block at −144px, live-previewing the DeltaChip and meter mid-drag; release commits IMMEDIATELY through the ONE setVerdict that every path shares (drag, Decide actionSheet with destructive-last Cancel row + separate Cancel card, and the medium/large-detent detail sheet's Keep/Pause/Cancel radiogroup), simultaneously re-bucketing the row, rolling the odometer (cancel MealKit Lite → $37.98/mo / $455.76/yr, badge 6→5, next-30 $117.45→$93.45), dimming-and-lifting the horizon bubbles, and re-deriving the Calendar tab's date-grouped charge rows and the Savings tab's reclaimed ledger (LensPro +$119.88/yr, FocusTimer +$47.88/yr) — with an exact-restore Undo toast in the sticky bottom:76 dock (undo-over-confirm, no timers). Fixtures are an arithmetic ledger: 12 subs = $137.93/mo = $1,655.16/yr, all aggregates derived live; the 45-char Streamly bundle name stresses ellipsis in row, sheet title, actionSheet header, and toast. Settings ships a whole-row 51×31 switch with a ≥3:1 off-track pair; per-tab scroll persists and re-tapping the active tab scrolls to top; drag disables under reduced motion leaving the button path. At desktop container widths (≥720px) it renders as a centered 430px phone column. Choose over mobile-price-drop-watchlist when the object is the user's OWN recurring-spend ledger being triaged verb-by-verb (keep/pause/cancel with reclaimed-money consequences), not external prices being watched; choose over mobile-banking-home-insights when the surface is a single-purpose audit bench whose every row mutates one verdicts map, not a broad accounts-and-insights home; choose over mobile-roundup-goals when money moves OUT of subscriptions via cancellation verdicts rather than INTO savings goals; choose over mobile-receipt-inbox when rows are forward-looking renewals on a 60-day horizon strip, not backward-looking receipts to file.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
