// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Kindling — Icebreaker Lab',
  description:
    "MOBILE (390px shell) dating-app opener workshop for Kindling: a 52px navBar (28px SparkMark flint-chevron, per-tab title, Saved 'Edit' / Sent 44×44 RefreshCw trailing) over a Lab tab whose sticky stack is exactly 172px — 52px toneBar (36px Playful/Curious/Bold segmented radiogroup with 8px pink/amber/indigo tone dots, arrow-key support) plus a 68px chipShelf ('MAYA · 6 CHIPS' overline pill, then 6 typed profile chips: 3 interest + 2 place + 1 food, Heart/MapPin/Coffee icons, 32px pills in 44px hits, drag ghosts at 0.9 scale) — then a 44px dashed STOCK FILLERS rail and a 3-card prompt deck where sentences flow at 16px/28 around inline slot-pill BUTTONS (dashed empty ⟨interest⟩, pulsing armed, amber-filled with clear-X). The signature move is typed composition with derived scoring: drop (or tap-arm/tap-place — the mandatory button path) a chip into a type-matching slot and ONE draftStore write re-derives the card's 3-segment SpecificityMeter (allFilled ? (allProfile ? Personal 3 : Specific 2) : (anyProfile ? 2 : Generic 1) — stock fillers can never reach Personal), fires a rising spark on tick-up, updates the labFooter's live meter + Preview button, and announces through the single sticky toastDock (bottom 76 = 64 tabBar + 12). Cross-tone switches void the draft with exact-snapshot Undo; the two-detent Preview sheet (55% / calc(100%−56px)) renders the draft as a received-side bubble in Maya's own pink chat theme ('____' for unfilled slots — the honest peek) and its large detent adds an Edit-message textarea, a 3-row SEND LOG, and 'Send to Maya' (#F59E0B fill, #451A03 text, 4.6:1) which appends to sent[] and recomputes EVERYTHING on the Sent tab: reply-rate bars grouped by score (3/4 · 75%, 1/2 · 50%, 0/2 · 0%), the '4 of 8 openers got replies · 50%' caption (4/9 → 44% after a send), the 'All 8 openers' terminal count, and the tab badge (4 = replies). Saved tab is a full editMode contract surface (Cancel/'2 Selected' live title/Done, 64px editToolbar with Delete LEADING and Duplicate trailing, 24px selectionCircles, execute-then-Undo deletes, live '3 SAVED · AVG 2.7' header, zero-button true-empty state); Sent refresh shows 4 deterministic 72px skeletons; the You tab derives sent/replies/rate stats and Sent-by-tone bars from the same arrays and holds two 51×31 switch rows. Desktop container widths (≥720px) render a centered 430px phone column, never a stretched relayout. Choose over mobile-caption-ab-lab when the workshop object is a compositional draft ASSEMBLED from typed tokens with a derived quality score, not an A/B crossfade between two finished variants; choose over mobile-parking-session when the sheet's job is previewing a composed artifact in the recipient's theme before an irreversible-feeling send, not writing one numeric value with cross-surface pricing consequences; choose over mobile-baby-care-log when lists are receipts of a creative loop (saved/sent with reply-rate derivations) rather than a chronological event log; choose over any desktop template when the brief needs a phone-first 390px shell with sticky chrome, bottom sheets, drag-with-tap-fallback, and 44×44 touch targets.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
