// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Choreo — Saturday Chore Streak Chart',
  description:
    "MOBILE (390px shell) family chore surface for Choreo, frozen at Sat Jul 4: a 52px navBar (check-with-footprints mark, IntersectionObserver-faded 'Saturday chores' compact title, 44×44 Refresh) over a 28px/700 large title and a live 'Sat Jul 4 · 11 of 15 done · 85 of 120 pts' caption whose arithmetic cross-checks by construction (Maya 5/6 + Leo 2/5 + Zoe 4/4; points 40+20+25 of 50+45+25), then a pinned 84px KidRingSwitcher (sticky top:52, radiogroup with arrow keys) of 48px per-chore SVG completion rings — one arc segment per chore with 3° gaps, brand done / amber claimed / ≥3:1 REST_GRAY pending, filling via 350ms stroke-dashoffset — around 36px initial discs. The Today card holds 60px chore rows that are deliberately NOT one button: a 44×44 check/hold target (28px 8px-rounded square), a central text button, an amber flame chip (400ms rAF count-up with translateY pulse), and a 44×44 ellipsis whose anchored menu is the mandatory visible path ('Mark done'/'Mark verified'); 'Clean bathroom' is a HoldToVerifyRow (900ms rAF conic sweep, cancel on early release or 8px movement, snap-fill under reduced motion) and 'Feed the dog' carries a 72px two-sided perforated SwapTicket inline beneath it (dashed divider, punched 8px notch circles, minHeight:72 so the state pill wraps at 320px). Tabs: Streaks (three 7-day StreakLadder cards — 32px day rows, 12px dots, 16px amber ShieldIcon 'Sick-day pass' tokens explaining unbroken gaps, Sat dot DERIVED live — plus a Family-best row), Swaps (three tickets with 'Offered'/'Awaiting parent'/'Approved'/'Voided' states, an execute-immediately-plus-Undo Approve, and a sticky 48px 'Propose swap' footer opening a two-detent 55%/calc(100%−56px) sheet whose 15-row chore picker is the one legal inner scroller, with a 36px segmented sibling radiogroup and blur-validated note field), and Family (44px utility rows; Approvals '2' pushes a stacked screen via one `screen` string with a 44×44 ChevronLeft back button). The SIGNATURE is one write rippling five surfaces: completing 'Feed the dog' runs the flame count-up 11→12, fills Maya's 6th ring segment, flips the inline SWP-03 ticket to 'Voided' with strikethrough, drops the Swaps badge 3→2, and fills Maya's Sat ladder dot while the header rewrites to '12 of 15' — and ONE Undo restores all of it because every surface derives from the {chores, swaps} snapshot, never stored twice. Refresh swaps the card to four deterministic 60px skeleton rows (60/45/70/60% + 40/55/30/40% bars, shared 1.6s shimmer removed under reduced motion) resolving to 'Updated just now' on the next user action, and per-tab scroll/screen state persists with re-tap-pops-to-root. Choose over mobile-parking-session when the surface is a multi-tab household ledger with per-person completion rings, streak ladders, and two-sided swap negotiation rather than a single live metered session with a countdown gauge; choose over mobile-exam-countdown-planner when streaks, sibling swaps, and parent verification — not time-budget replanning across a countdown spine — are the story; choose over mobile-roundup-goals when the moving quantity is chore completion and streak credit rather than money routed between savings jars; choose over any desktop template when the brief calls for a phone-first 390px shell with sticky navBar/tabBar, bottom sheets, hold gestures with button fallbacks, and 44×44 touch targets.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
