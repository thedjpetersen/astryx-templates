// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Gantry — June Trips Toll Recap',
  description:
    "MOBILE (390px shell) toll-statement recap for the fictional Gantry transponder app: a 52px navBar (24px arch-G brand mark, IntersectionObserver fade-in compact title, 44×44 Share2 export button) over a 52px large-title row ('June Trips' 28px/700 + a disabled single-month stepper labeled 'May/July statement not in demo'), a monthSummaryCard whose every figure derives live from ONE 11-trip / 34-crossing / 405-mi fixtures table ($78.00 hero; '$73.75 charged · $4.25 pending (2 disputes)' split; 11 trips / 34 crossings / 405 mi stat row wrapping 2+1 below 350px), and a TRIP LEDGER of 11 tripCards (60px expand headers, trips 1–2 open by default). Each expanded trip stacks three custom components: an 88px scrubable RouteRibbon (2px dashed lane, mile-marker-positioned SVG arch glyphs in 44×44 buttons, 20px resting fee bubbles — dashed brand outline when disputed — an inflated 56px detail card that clamps via translateX(−frac·100%), a running '$6.25 of $9.50' subtotal chip, 45° hatched express-lane strips, same-mile duplicates stacked into one ×2-chip arch whose tap cycles the pair, and role='slider' ArrowLeft/Right keyboard parity), 60px DisputeFlagRows (swipe to a −72px Dispute block, release past −120px files immediately — reversible so never a confirm — plus the mandatory 44×44 ellipsis opening a centered-no-icons actionSheet), and a 72px TripCostStackbar (tolls/fuel-est/parking flex segments in brand-cyan/teal/amber with contrast math, a 2px reconcile seam at the expected-tolls boundary when charged ≠ expected, and the disputed fee SUBTRACTED from the bar and re-rendered as a detached dashed escrow slice 4px right of the bar end). The signature move is the dispute cascade: one FILE_DISPUTE reducer write moves money from charged to pending in the header ($73.75→$71.25 for a $2.50 crossing), stripes the row and swaps its fee to a dashed '$2.50 pending' pill, detaches the stackbar slice, bumps the Disputes tab badge 2→3, and drops the Export sheet's live count 'Export 32'→'Export 31' — with a persistent no-timer Undo toast in the sticky-in-flow toastDock (bottom 76, above the 64px 4-tab tabBar). Tabs persist state (per-tab scrollTop, expansion; re-tap scrolls to top); Disputes shows 72px under-review cards (or a FlagOff true-empty state after withdrawing both), Passes shows the GNT-4471-A transponder + posted rate table, Export shows history + 'New export…' into the two-detent (55% / calc(100%−56px)) Export sheet with CSV/PDF radiogroup, 'Show 27 more' loadMoreRow, 'All 32 line items' terminal caption, and a 48px footer button. At desktop container widths (≥720px) it renders as a centered 430px phone column with hairline borders, never a stretched relayout. Choose over mobile-parking-session when the object is a monthly STATEMENT to reconcile and dispute (ledger of trips, per-trip cost splits, escrow arithmetic), not a single live metered session with a countdown dial; choose over mobile-transit-companion when rows carry money that moves between charged/pending buckets rather than departures to ride; choose over mobile-newsletter-triage when the batch verb is dispute-with-instant-escrow rather than roll-up/unsubscribe; choose over any desktop template when the brief needs a phone-first 390px shell with sticky navBar/tabBar, bottom sheets, swipe-reveal rows, and 44×44 touch targets.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
