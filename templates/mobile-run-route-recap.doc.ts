// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Pacelight — Run Route Recap',
  description:
    "MOBILE (390px shell) single-run post-run analytics surface for Pacelight where ONE distanceCursor threads every module: a 52px navBar (44×44 'Activity' back, 18px bolt mark + 'Morning Run' whose accessible h1 carries the full 'Morning Run — Riverside Bolt Loop', 44×44 Share → 'Link copied' toast) over a full-bleed 390×260 STYLIZED SVG route ribbon (no map tiles) — the bolt-shaped polyline rendered as 84 individually-stroked round-cap 6px segments colored by pace (fast = Pacelight green ramping to slow amber; zero-sum ±4 s wobble makes neighbor segments visibly discrete), 8 km-marker dots, a start dot, a finish chevron rotated to the last segment, and a 16px brand puck inside a 44×44 HTML hit that IS a role='slider' keyboard scrubber (±100 m arrows, ±1 km PageUp/Down, Home/End, full aria-valuetext '4.8 km — 5:16 per km, 68.0 m elevation, 168 bpm'). Below: a 64px readout row (44×44 prev/next-km buttons, disabled at 0/8400, flanking a 44px tabular chip that drops bpm to a second 11px line under 360px), a 2×2 stat grid whose tiles are cursor buttons (DISTANCE 8.40 km→finish · TIME 42:00→start · AVG PACE 5:00→PB km · ELEV GAIN 76 m→peak; all four derive live from the arrays — 2520 s / 8.4 km = exactly 300 s/km), an 'Avg HR 159 bpm · Max 168 bpm' caption (13356/84 exactly), a 9×60px diverging split ladder (bars grow from a shared 1px axis, faster left in green, slower right in amber; km 5's +20 s bar fills the measured half zone EXACTLY; km 7 wears the FlagIcon PB badge; the 0.4 partial renders a 40%-opacity outlined bar and '1:56'), and a synced 140px elevation strip (85-point area sparkline, 8px 'you are here' column + 1px brand line, peak dot '68 m'). Setting cursorM from ANY writer — puck drag, slider keys, prev/next buttons, split-row tap (jumps to the km midpoint), elevation-strip drag, stat tiles — simultaneously moves the puck, slides the elevation column, rewrites the chip, and highlights the active split row (brand 10% wash + 2px leading bar, clamping to row 1/row 9 at the 0/8400 boundaries). The only overlay is the per-split anchored ellipsis menu (Jump / Copy split time → 'Split 5 time copied — 5:20' / Pin split executing immediately with an Undo toast per undo-over-confirm); the single polite toast dock is STICKY-IN-FLOW (bottom 16) with no auto-dismiss timers. At desktop container widths (≥720px) it renders as a centered 430px phone column with hairline borders — never a stretched relayout. Choose over fitness-activity-feed when the brief is ONE run's connected analytic story (route ↔ splits ↔ elevation ↔ HR through a single cursor), not a social stream of many workouts; choose over workout-session-logger when the user is reviewing a finished effort, not entering sets and reps; choose over mobile-sleep-morning-report when the scrubbed spine is a geographic route ribbon with pace/elevation physics rather than a time-axis hypnogram; choose over any desktop template when the brief calls for a phone-first 390px shell with 44×44 touch targets, sticky navBar, and a sticky toast dock.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
