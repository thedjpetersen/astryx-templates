// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Lume — Story Planner',
  description:
    "MOBILE (390px shell) close-friends story planner for Lume: a 52px navBar (Lume crescent mark, 'Tonight's story' h1, an 'Inner 8' audience pill with a 16px UsersIcon) over a Plan tab holding a 32px runtime subrow ('Runtime' · Edit toggle · a '53s / 60s' tabular pill that flips to the error pair at exactly 60s), a horizontal snap-x filmstrip of four reorderable 72×108 id-derived-gradient thumbs (20px brand order badges — white on #B4457E ≈ 5.9:1 — plus 20px duration chips; 4×72 + 3×8 + 16 = 328 at 390), a 64px DurationBalancer whose 8px track renders one brand-ramp segment per frame in framesOrder order (12s=20% + 18s=30% + 8s=13.33% + 15s=25% = 88.33% filled, the 7s = 11.67% remainder in a ≥3:1 rest fill, a 2×12 error notch at the 60s mark, '7s unused / 60s cap' caption), a frame-detail listCard of four 116px rows (48×72 thumb, 'Frame n of 4 · 12s', a 96×32 spinbutton stepper min 3s / max 20s whose plus half disables at 35% opacity when 53→60 headroom is gone), and a 60px AudienceVennChip ('Inner 8 · 3 excluded'). The signature move is one-array authority: drag frame 3 ahead of frame 1 (pointer drag with 8px slop, siblings sliding a slot; or the mandatory edit-mode 44×44 move-left/right chevrons) and the order badges, balancer segments, detail rows, and the Scheduled draft's lead 48×72 thumbnail all re-sort from the single framesOrder write, announced by the one sticky-in-flow toast dock ('Golden hour rooftop moved to position 1'). The Audience tab lists 8 included + 3 excluded = 11 close friends as 60px whole-row role=switch buttons (51×31 switches, excluded avatars at 55% grayscale; Rosa Lindqvist-Åkerström is the truncation stress); the Scheduled tab's draft row live-mirrors the Plan order and its terminal caption '3 stories · 128s planned' re-derives (53+47+28). A two-detent (55% / calc(100%−56px)) 'Close friends' sheet with a ghost row of excluded avatars threads every toggle through the same owner — exclude Maya Okafor and the chip, navBar pill, section headers, and Scheduled 'Inner n' all read 7. Edit-mode Remove frame obeys undo-over-confirm (remove f3: 53−8 = 45s; Undo restores the exact position and 53s). At desktop container widths (≥720px) it renders as a centered 430px phone column with hairline borders, never a stretched relayout. Choose over mobile-podcast-queue when the reorder target is a short creative sequence against a hard time CAP (budget bar, per-item duration steppers, audience gating), not a long listening queue with grip-drag rows and an end-time ledger; choose over mobile-parking-session when the surface is compositional planning with a filmstrip and include/exclude circle, not a live metered session with a countdown gauge and tiered pricing; choose over mobile-meal-quickadd-log when items are ordered story frames feeding one 60s budget, not repeated quick-add foods feeding macro arcs; choose over any desktop template when the brief calls for a phone-first 390px shell with sticky navBar/tabBar, a bottom sheet, drag-reorder with a button path, and 44×44 touch targets.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
