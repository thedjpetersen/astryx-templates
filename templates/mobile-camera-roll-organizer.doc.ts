// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Shoebox — Camera Roll Organizer',
  description:
    "MOBILE (390px shell) camera-roll triage surface for Shoebox: a 52px navBar (24px ShoeboxMark, compact title + hairline both WIRED to an IntersectionObserver sentinel, brand-text Select/Cancel toggle) over a 52px 'Library' large-title row with a live tabular '47 photos' caption, then a full-bleed month-sectioned contact sheet — 9 day-clusters across June/May/April 2026 (19+17+11 = 47 photos), each cluster a 4-col/2px-gap/dense-flow grid promoting its first shot to a 2×2 feature tile ((390−6)/4 = 96px thumbs, 194px feature) with an overlaid day-caption chip whose count DERIVES from the live cluster length ('SAT, JUN 28 · 7'). Thumbnails are deterministic hueIndex gradients (no photo assets), every tile a real <button> named by its subject. A 3-tab 64px tabBar (Library / Albums / Duplicates with a live 4-set badge) swaps for a 72px selectionTray in selectMode: 44×44 removable chips around 32px mini-thumbs, a 17px/700 tabular count, and a 36px 'Add to album' brand button (disabled 'Select photos' state at zero). The signature flow runs Select (or 450ms long-press) → tap 3 thumbs → 'Add to album' → the 55%-detent album-picker sheet (grabber toggles to calc(100%−56px), 6 × 60px album rows + an honest 'New album' stub) → pick Hikes → the 3 tiles dissolve 160ms out of the grid, c-jun28's chip 7→4, June 19→16, the large-title 47→44, Hikes 24→27, the Albums badge shows 3, the tray unmounts, and the ONE sticky-in-flow aria-live toast announces '3 photos moved to Hikes'. The Duplicates tab derives '4 sets · 8 removable shots · frees 20.2 MB' from four fanned dupeStack cards (−3°/0°/+3° gradient fans, amber Star on the best shot, 2-shot minimum fan for ds-d) whose 'Keep best' removes the discards everywhere — grid, chips, badge, MB summary — and 'Keep all' marks the set reviewed out of the math. Albums push a seeded 4-col detail grid (back button navBar, 200px title ellipsis); a full-screen photoCover (z50) offers Heart-favorite and a single-photo 'Move to album' path that closes the cover before opening the sheet. Stress fixtures live in data: the single-photo c-jun05 cluster ('THU, JUN 5 · 1'), the 8-photo c-may30 dense-flow wrap, the 47-char 'Pacific Northwest camping & fire lookout trips' album row, and the two-line-wrapping whiteboard subject. At desktop container widths (>520px) it renders as a centered 430px phone column with hairline borders. Choose over mobile-parking-session when the object is a MEDIA LIBRARY triaged by batch selection, grid dissolves, and duplicate verdicts, not a live metered session with a countdown gauge; choose over mobile-transit-companion when the surface is a full-bleed photo grid with a selection tray, not inset departure boards under a trip sheet; choose over mobile-p2p-payment-flow when the primary verb is organize-and-file across tabs rather than a linear send flow; choose over media-asset-pipeline or any desktop template when the brief calls for a phone-first 390px shell with sticky navBar/tabBar, bottom sheets, long-press selection, and 44×44 touch targets.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
