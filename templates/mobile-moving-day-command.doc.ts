// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Vanward — Moving Day Command',
  description:
    "MOBILE (390px shell) dependency-aware moving-day command board for Vanward, frozen at 'Now 1:40 PM' against a 5:00 PM keys deadline (200-min window): a 52px navBar (ramp-chevron brand mark · slack-tinted CountdownPill 'Keys 5:00 PM · −30 min' · 44×44 Re-plan) over a Board-only 52px 'Moving Day' large-title row and a 96px PressureHeader whose flex-grow PhaseBlockTrack sizes four phase blocks by remaining minutes (Pack 50 / Load 80 / Clean 55 / Handoff 45 = 230 vs 200 → a red '−30 min' pill overhangs the trailing edge and the track gains a red border); 18 tasks in four inset-grouped phase cards as 60px rows — chain-LOCKED rows (20px LinkIcon, secondary 'Locked — finish Disassemble couch', aria-disabled with 'locked' in the name) cascade-unlock when their blocker completes, announced by the single toastDock ('Disassemble couch done · Load couch unlocked') — with swipe-right-to-complete at +72px (SLACK_GREEN Done block), the leading 24px toggle circle as the visible twin, and a 44×44 ellipsis menu (Complete / Defer / Assign to…) as the mandatory button path. The Truck tab renders an SVG cross-section (viewBox 358×200, 12×5 grid of 24px blocks at 20 cu ft each): 9 loaded brand blocks + projected fill to 62 blocks puts a 2-block hatched red 'WON'T FIT · 40 CU FT' band above the roofline ('Projected 1,240 of 1,200 cu ft — 40 over'), with a Now/Projected radiogroup and 44px legend rows. The signature move is computed triage: the two-detent (55% / calc(100%−56px)) TriageSheet — opened from the sticky 'Running behind?' CTA (bottom 64, above the 64px tabBar) or the navBar Re-plan — proposes a fixed 40-minute cut list (defer garage tools +20, defer garage misc +15, merge the wipes into Full wipe-down +5) whose Accept re-flows EVERYTHING from the one task graph: track re-flows to Pack 15/Load 80/Clean 50/Handoff 45 + an 18px '+10' slack cap, the pill flips green '+10 min', the truck drops 280 cu ft to 'Projected 960 of 1,200 — fits with room' (48 blocks, 80%), Crew rows grow 'Moved to Clean' chips (Dev, Priya), and Board grows a 'Deferred · 2' pill section whose 44px Restore buttons re-add minutes live (restoring tools alone flips slack back to −10 red) — all with exact-snapshot persistent Undo ('Previous plan restored') through the sticky-in-flow toastDock, no confirm dialogs anywhere. Per-tab scroll persists, active-tab re-tap scrolls to top, and at ≥720px container width it renders as a centered 430px phone column. Choose over mobile-parking-session when the object is a dependency-graphed PLAN under a deadline (cascade unlocks, phase pressure track, computed cut list), not a single live metered session with a countdown dial; choose over mobile-subscription-audit when triage means accepting one computed re-plan that re-flows a schedule and a truck, not verdict-swiping rows into keep/pause/cancel buckets; choose over mobile-date-night-builder when time pressure is a deficit against a fixed deadline with a proposed cut list, not drag-retiming stops on a proportional evening rail; choose over mobile-transit-companion when the bottom sheet DECIDES the plan (accept/undo consequences across three tabs) rather than tracking a live trip's progress.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
