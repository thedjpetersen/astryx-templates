// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Kestrel — Price-Drop Watchlist',
  description:
    "MOBILE (390px shell) trading-desk-flavored price-drop watchlist for Kestrel (amber #D97706, quarantined as the light-dark(#B45309, #FBBF24) text pair): a 52px navBar (28px eye mark with a downward tick-arrow pupil, IntersectionObserver fade-in 'Watching · 14' compact title, 44×44 Plus + Refresh) over a 52px large title with an 'Updated 8:05 AM' caption, a 44px DropTicker (three cross-checked events — 'Arc Floor Lamp −18% overnight · now $149' — cycled by its own 44×44 chevron button, no timers, 24px fade mask only when the line measurably clips), then inset-grouped sections of 64px SparkbandRows ([text flex][100px sparkline][64px price col], 130+100+64+32 = 326 at 390px ✓): READY TO BUY · 3 trailing '$109 under list' (71+16+22 ✓, green light-dark(#15803D,#4ADE80) prices over strikethrough was-prices), WATCHING · 9 trailing '$246 to targets' (14+50+34+12+50+38+19+7+22 ✓), EXPIRED · 2 dimmed rows with 36px Rewatch buttons, 'All 14 items' terminal caption, and a 64px tabBar (Watchlist badge = ready count, Drops, Alerts, Profile — per-tab scroll persisted, active re-tap scrolls to top). Every sparkline carries a shaded amber target band whose top edge wears a draggable 44×44 grip: status is DERIVED (ready = !expired && target >= current), so dragging Walnut Desk Shelf's band past its $139 market price FLIP-migrates the row (240ms transform spring, instant under reduced motion) from Watching #1 to Ready #4 while READY·4/WATCHING·8, badge 3→4, '$246'→'$232', a 'Target hit' ticker event, and a no-timer Undo toast (restores the exact $125 drag-start target) all re-derive in one render. Row tap (the gesture's button path) opens the 55%/full-height target sheet: deterministic-hue monogram tile, 120px sparkline, snap-to-detent TargetScrubber (role=slider, $109–$153 for Walnut, Low/30-day/−10% detents with $2 magnetic snap + 120ms tick pulse, ←/→ ±$1, PageUp/Down detent jumps), a 96×32 ±$1 spinbutton stepper with ±5% secondaries disabling at range edges, a live notification-preview row that flips to 'Ready now' copy below market, and a 'Stop watching' row behind the sheet ellipsis that executes immediately with index-exact Undo (undo-over-confirm). Drops tab: 6 cross-checked feed rows that jump-and-ring-flash their watchlist row across tabs; Alerts tab: three whole-row 51×31 switches (off-track ≥3:1 pair) + quiet hours; add sheet appends suggestions and every count re-derives. Desktop container widths (≥720px) render a centered 430px phone column. Choose over mobile-parking-session when the surface is a many-row LIST whose rows migrate between derived-status sections, not a single live session with one dial and a ledger; choose over mobile-banking-home-insights when the signature mutation is per-row threshold editing (drag a band, watch the row change sections) rather than one global filter threading a ring, rail, and ledger; choose over mobile-podcast-queue when rows move because DATA crossed a user-set line, not because the user reordered them by hand.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
