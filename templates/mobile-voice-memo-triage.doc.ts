// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Murmur — Voice Memo Triage',
  description:
    "MOBILE (390px shell) voice-memo inbox for Murmur where recordings are ore to be mined: a 52px navBar (24px MurmurMark — four waveform bars reading as an opening quotation mark — a fading 'Murmur' compact title wired to an IntersectionObserver sentinel, and a 44×44 capture button whose 28px magenta circle + static record ring only toasts 'Recording is disabled in this demo'), a 52px 'Inbox'/'Processed' large title over a 24px DERIVED stats caption ('5 memos · 6:12 total · 4 of 13 items mined' — durations 47+84+33+125+83 = 372 s, handled 1+2+0+1+0 = 4 of 3+3+2+4+1 = 13), an inset listCard of five 110px memoRows (16/500 title vs tabular duration, two-line 13px transcript peek at lineHeight 18, a '2 of 3 mined' progress pill that flips to brand fill when complete, and a separate 44×44 ellipsis opening an actionSheet — Play from start / Mark processed / Delete memo destructive-last with its own Cancel card; delete executes immediately with an Undo toast, never a confirm), and a 64px two-tab bar whose Processed badge (3) is derived from the order array. Tapping a row opens the two-detent transcript sheet: MEDIUM (55%) locks a word-level TranscriptScrubRow — 39 fixture words with explicit monotonic ms pairs — to a 96-bar deterministic WaveformBars playhead (the BLOCK is role='slider': ArrowLeft/Right word steps, Home/End, aria-valuetext \"0:23 — 'dentist'\"), with a 56px transport (±5s word-snapping nudges, brand play circle, play-from-top) whose elapsed label is rAF presentation only; LARGE appends the extraction workspace — a Mark-span toggle arms tap-first-word/tap-last-word range selection (drag works via the same elementFromPoint machinery), a floating typePicker mints Task/Idea/Errand chips, per-sentence 36px Extract buttons give the non-gesture parity, ConfidenceInk words (Sarah 0.61 / Priya 0.72 / cleaning 0.78) cycle alternates from 44px review rows and rewrite covering chip quotes in the same frame because chips store ranges and derive text from the words array. Extracting the final span processes the memo in one action: the row slides out of Inbox (transitionend-finalized ghost, fade under reduced motion), the badge increments 3→4, both captions re-derive (Inbox 4:48 · '2 of 10 items mined'; Processed 5:44 · '10 items extracted'), the waveform keeps permanent highlight bands, and the sticky-in-flow toast dock offers Undo — which restores every derived number in one snapshot assignment. At desktop container widths (≥720px) it renders as a centered 430px phone column. Choose over mobile-podcast-queue when the audio object is a TRANSCRIPT to be mined into typed spans (word-level scrubbing, span chips, confidence repair), not a queue to be reordered and time-budgeted; choose over mobile-parking-session when the sheet's job is a reading/extraction workspace over inner-scrolling content rather than a single priced value with cross-surface preview; choose over mobile-dm-request-triage when triage means decomposing one item's content into artifacts, not accept/decline routing of many items; choose over any desktop template when the brief needs a phone-first 390px shell with push-free tabs, bottom sheets, and 44×44 touch targets.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
