// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Podium — Trivia Host Console',
  description:
    "MOBILE (390px shell) single-task pub-trivia MC console for Podium, frozen at The Dog & Duck, Round 3 of 6, Q4 of 5: a 52px navBar (R3/6 + five 6px question dots as one status element, venue h1 ellipsized at 200px, gavel with a '2' pending-dispute badge, settings menu) over a stage-gated RevealStager card — 'ROUND 3 · Q4 OF 5' overline + SCIENCE chip, the 22px/700 question 'Which planet has the most confirmed moons?', a 4-pip Read → Options → Lock → Reveal stageRail with aria-current='step', and four 44px A–D option rows that stay hidden at 'read' ('Options hidden — read the question aloud'), grow LockIcons + an 'Answers locked' chip at 'locked', and tint row B (Saturn) success-washed with a CheckIcon at 'revealed'. Below, an 8-team TeamScoreTicker of 64px transform-positioned rows (rank 24px col, name over '11 correct · TB 3.4s' meta, 28px AnswerFlipChip, 34px RankDeltaChip, 17px/700 tabular score) whose pre-reveal sum 135 = 68 correct × 2 − 1 host adjustment; the single reveal dispatch pays 5 teams +2 (sum 145), rotateY-flips every chip to its letter, FLIP-reflows ranks over 300ms (t3 ↑1 past t2 on TB 4200 < 5100, t7 ↑2), fills the Q4 navBar dot, enables the stage-gated Adjust steppers (96×32, min 0/max 99, spinbutton ArrowUp/Down), and flips the DisputeQueueSheet banner to 'Q4 now disputable'. There is NO tabBar — a 64px StageBar always names the next legal action ('Show options' → 'Lock answers' → 'Hold to reveal' [800ms pointer charge, early release resets, 'Reveal now' in the ⋯ menu as the button path] → 'Next question' → 'Round 3 complete · Review disputes'), with 'Skip question' as a reversible Undo-toast action. The two-detent (55% / calc(100% − 56px)) DisputeQueueSheet resolves undo-over-confirm: Grant executes immediately — Tequila Mockingbird +2 on R3·Q2 jumps 17 points past BOTH 16-point teams (rank 7 → 5, sum 147) behind the medium detent — with a sticky-in-flow toastDock Undo restoring the exact 145-state and queue position; Deny moves Beer Pressure's 2-line-clamped claim to Resolved with a Denied badge. Q5 (B Brazil) pays 5 more (+10, sum 157) and lands a triple 24-point tie ordered by tie-break 3.4s/4.2s/5.1s with three TB badges. 'End game' routes through the one centered alertdialog (Cancel-first focus, scrim click never dismisses). At desktop container widths (≥720px) it renders as the standard centered phone column. Choose over mobile-parking-session when the surface is a live event CONSOLE whose core is a legal-order stage machine gating every other affordance, not a metered session with pricing math; choose over mobile-group-gift-pool when mutations are retroactive rescores with rank-exchange animation, not contribution progress toward a funding target; choose over mobile-hotel-key-checkin when the bottom chrome must be a next-legal-action StageBar with a hold-to-commit primary rather than stage-readiness cards; choose over any desktop template when the brief demands a 390px shell, bottom sheet, sticky toast dock, and 44×44 touch targets.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
