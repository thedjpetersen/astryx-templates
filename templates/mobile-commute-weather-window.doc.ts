// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Nimbra — Commute Weather Window',
  description:
    "MOBILE (390px shell) 'when should I actually leave' commute surface for Nimbra where EVERYTHING is one deterministic function of a single departure playhead (selectedDepartureMin, default 500 = 8:20): a 52px navBar (umbrella-of-bars mark, 'Nimbra' over the LIVE 'Leave 8:20 · light drizzle' subtitle, 44×44 refresh) over a 72px departureSummary (48px brand leave-at chip '8:20 / arrive 8:45' + 36px 'Best window · 10:40' button), the 144px DepartureScrubStrip — a 1000px horizontal rail (36 × 28px − 8 ✓) of 20-min precip bars (level × 24px, three light-dark fills with the ≥3:1 data-fill edge rule) and gust dots at y = 88 − gust×2 (gust = 8 + 10·level exactly, peak 38 at 13:20), hatched dry-gap underlays, and a 28px playhead handle in a 44×44 target whose pointerup magnet-snaps to the minimum-wetness departure within ±60 min (ties → earliest: releasing at 8:05 lands exactly 8:20) — a 44px ±15m stepper row (spinbutton + focusable role=slider rail with ←/→ ±5m and Enter = snap as the button path), the 152px RouteRibbon (stylized home→office SVG, 120/280/100 path units = 25 min × 20u ✓, each leg recolored by the precip level at ITS OWN pass-through midpoint with floating time labels 8:23/8:33/8:42), three 72px ExposureLegRows scoring level × exposedMinutes live (0+0+5 = 5 of 39 at default; 18+6+10 = 34 at the 13:20 stress) with 'of 18/6/15' scale captions, a Covered shelter badge, and expandable 44px sampled-tick detail lines, and a gear teaser whose count chip (1 → 3 → 1 across 8:20/7:40/10:40) opens a medium-detent (55%) gear sheet that re-sorts recommended-first live via an embedded compact ±15m stepper while the shell scroll-locks (focus enters with preventScroll). Commutes tab: three saved-route 72px rows (6+14+5 = 25 ✓, 4+10+4 = 18 ✓, 12-min walk; the 46-char 'Home → Riverside Innovation Campus (North Gate)' truncation stress) with live wet/dry pills — Home→Office is LINKED to the live playhead, so scrubbing to 10:40 drops the Commutes tab badge 2→1 — plus push-in-place detail screens, per-row ellipsis menus with execute-then-Undo 'Remove route' (exact-index restore, 'Restored' confirmation), and an 'All 3 routes' terminal caption. Alerts tab: 3 × 60px severity-swatch rows, 2 unread driving the badge, tap-to-read. RefreshCw swaps the legs card for three deterministic 72px skeleton rows (60/45/70 + 40/55/30 widths) that resolve on the next user action with 'Updated just now'. One sticky-in-flow polite toastDock (bottom 76 above the 64px tabBar) announces snap results, refresh, undo, and settled totals; per-tab scrollTop/expansion state persists; re-tapping the active tab pops to root. At desktop container widths (≥720px) it renders as a centered 430px phone column, never a stretched relayout. Choose over mobile-transit-companion when the object is a weather-conditioned WHEN-to-leave decision (scrub a forecast, watch wetness scores/gear/badges re-derive) rather than live transit tracking; choose over mobile-parking-session when the signature control is a horizontal forecast scrubber with snap physics feeding cross-tab derivations, not a radial meter with a paid-time extend sheet; choose over mobile-allergy-season-radar when the driver is a draggable departure time recomputing a multi-leg route, not a fixed daily environmental report.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
