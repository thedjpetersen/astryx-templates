// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Pitchloft — Brand-Deal Pitch Inbox',
  description:
    "MOBILE (390px shell) creator brand-deal inbox for Pitchloft that negotiates with numbers: a 52px navBar (envelope-flap-over-coin mark · per-tab title · 44×44 Refresh) over a pinned 76px MonthIncomeStrip rendered on ALL four tabs — a 12px segmented bar deriving booked $3,600/9,000 = 40.0% (brand) + pending $900 = 10.0% (amber) with three 44px legend buttons that jump to Booked, the AWAITING REPLY header, and the Profile goal row — then a 4-tab tabBar (Inbox badge 6 · Booked badge 1 · Rates · Profile). Six pitches whose offers cross-check to the '$9,700 on the table' subtitle group under STRONG FIT (Lumira 92, TrailKit 90) / WORTH A COUNTER (Brewtide 77, Zenda 74) / BELOW YOUR FLOOR (Quantiq 46, GlowPop 46) as 72px monogram rows with 36px mini FitScoreMeters — a 270° gauge split 108°/108°/54° for niche/offer/scope maxima 40/40/20, offerFactor = min(40, round(40·offer/baseline)) with Lumira's raw 44 visibly capped at '40/40 · +$150 above your card — capped'. Rows expand inline to a 96px meter, three tappable factor rows with explanation lines, an OfferDeltaChip ('−$1,150 vs your rate card ($2,850)' is the 320px wrap stress), per-deliverable rate-card prices, and Decline (leading) / Counter / Accept (trailing). The signature move: swiping a pitch RIGHT past 72px (or the 44×44 ellipsis actionSheet, or the expanded Counter button) opens a 55%-detent counter sheet pre-filled at the $1,200 rate-card baseline; dragging the RateSlider (range 1050–1500, step $25, flanked 44×44 ∓ buttons, ticks at offer and baseline) live-rederives the projected score (35→77 at $1,050 proving the resting math, 40→82 capped at $1,200 AND $1,500 with a 'Fit maxed at your rate' caption), flips the delta chip red→'$0'→green across baseline, and appends a dashed projection segment + '+$1,200 if accepted' caption to the pinned strip. Sending regroups the row into a COUNTERED section, restamps its chip 'Countered at $1,200 (+$150 over offer)', moves pending to $2,100 (23.3%), bumps the Booked badge 1→2, and stamps the Reel line 'Last used in a counter · Jul 4' — while sent counters snapshot their rate, so stepping Reel to $1,300 in Rates rederives only OPEN pitches (TrailKit 90→89). Decline/Accept execute immediately with a persistent no-timer Undo toast in a sticky bottom:76 dock; accepting Brewtide reflows the strip to $4,650 (51.7%), and stepping the Profile goal down to $5,000 after accepting Lumira exercises the over-goal clamp ('$6,400 of $5,000 goal · +$1,400 over'). Refresh swaps the inbox for four deterministic 72px skeleton rows resolved by the next user action. Desktop container widths (≥720px) render a centered 430px phone column. Choose over mobile-dm-request-triage when the inbox object is a priced NEGOTIATION scored against the user's own rate card (counter sheet, income strip, projected-fit math), not a message-triage queue; choose over mobile-price-drop-watchlist when the money math is bidirectional offers/counters with a pinned goal ledger rather than tracked external prices; choose over mobile-parking-session when the surface is a multi-tab inbox with per-tab persistence and cross-tab consequence chains, not a single live-session dial; choose over mobile-receipt-inbox when rows carry decomposable scores and negotiation verbs instead of archival amounts.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
