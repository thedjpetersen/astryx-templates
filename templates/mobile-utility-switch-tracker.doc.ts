// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Switchstead — Utility Switchover Tracker',
  description:
    "MOBILE (390px shell) move-day utility switchover tracker for Switchstead, frozen around the fixed move date Fri Jul 18 (day 4 of a 13-day Jul 14–Jul 26 window; all dates are integer indices formatted via WINDOW lookup — no Date objects): a 52px navBar (two-prong house mark, 'Move · Fri Jul 18', and a 26px health pill button — '6 dark days' red at rest, green '0 dark days' when clear — that jumps to the worst-risk card) over three sticky-tab surfaces. SERVICES renders a 64px From/To addressStrip (41 Alder Ct, Apt 2B → 128 Foxglove Ln), a live 'Dark days 6 · Double-pay $20.00 · 1 of 5 seamless' summary strip, and five risk-sorted 112px serviceCards — each a dual-address CoverageBars pair (10px percentage tracks around the 30.77% MOVE marker: old fill ends at oldStop, new fill starts at newStart, red 45° gapHatch spans dark days, amber overlapTint spans double-paid days) with a derived StatusBadge (trash '4 dark days' red → internet '2 dark days' red → gas '$18.00 double-pay' amber, the long-name truncation stress → water '$2.00 double-pay' → electric 'seamless'), expanding to confirmation-code/copy, install-window pill ('Jul 20 · 8:00–12:00'), and a 51×31 full-row needs-you-home switch. The signature move is the two-detent (55% / calc(100%−56px)) DateAdjustSheet: two 13-pill snap rails (56×44 buttons) with mandatory 44×44 chevron steppers and role=spinbutton readouts write through ONE store on every commit, so the sheet's own CoverageBars preview, the card badge (internet ladder: 2 dark → 1 → seamless → $4.50 → $9.00 double-pay), the navBar pill (6→5→4), the summary strip, the TIMELINE rows (11 = 2×5 services + move day, grouped under weekday headers with amber 'be home 8–12' pills and a 'Needs you home: 2 windows' caption), and the DOCS subtitle ('LM-90342 · starts Jul 20') all re-derive in the same render; the services list re-sorts by risk only on sheet close, never under an open sheet. Remove service (ellipsis anchored menu, destructive last) executes immediately with a persistent Undo toast that restores the exact sort position — dark days 6→2, Docs badge 6→5, timeline 11→9, monthly total $459→$426 and back. One sticky-in-flow aria-live toast dock (absolute bottom:76 only during sheet scroll-lock), scroll-locked shell while the sheet is open, per-tab scroll persistence, and a centered 430px phone column at desktop container widths (≥720px). Choose over mobile-moving-day-command when the object is the UTILITY HANDOFF LEDGER around a move date — per-service coverage bars, gap/overlap arithmetic, and provider paperwork — not a dependency-chained task board racing a deadline; choose over mobile-parking-session when the surface tracks five parallel service windows on a shared 13-day axis rather than one live metered session with a countdown dial; choose over mobile-thermostat-schedule when bars encode COVERAGE DATES with derived risk badges, not recurring daily setpoints; choose over any desktop template when the brief calls for a phone-first 390px shell with sticky navBar/tabBar, a bottom sheet, 44×44 targets, and undo-over-confirm.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
