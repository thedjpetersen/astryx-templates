// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Encore — Concert Night Companion',
  description:
    "MOBILE (390px shell) show-night logistics surface for Encore, covering one concert — Velvet Static at The Foundry, Portland: a 52px navBar (44×44 ticket-stub brand mark, 'Velvet Static · The Foundry' h1, 44×44 Share) over a sticky 64px NowPlayingStrip (pulsing live dot — static under reduced motion — 'Static Bloom', '9 of 20' tabular meta, and a 3px 45%-filled progress bar whose width and label BOTH derive from nowIndex 9/20; the whole strip is a button that jumps to the setlist and centers the now-row), four tab panels behind a 64px tabBar with a derived Crew badge '3'. SETLIST: a custom full-bleed SetlistProgressRail — 20 connected 60px rows over a fixed 56px spine column (played 20px check nodes, a 28px pulsing brand now-node, upcoming 20px rings on an explicit ≥3:1 light-dark(#B8AEA4,#6E645C) border per the rest-fill amendment), a 44px 'ENCORE BREAK · 4 SONGS' divider after row 16, exactly two drift pills ('moved up 3' on Neon Ritual, predicted #8 → slot 5; 'moved back 2' on Glasshouse, #10 → 12, both with contrast math), a 'Meet here' flag chip pinned to afterSongIndex, per-row 60px inline expansions, a 44×44 'Mark next song' advance (strip flips to '10 of 20' / 50%, played+now+upcoming re-checks 9+1+10=20), and a live-derived terminal caption '20 songs · 84 min planned' (durations sum to exactly 5040 s). VENUE: the signature move — a stylized SVG floorplan (viewBox 0 0 358 280: Stage, The Pit ellipse, Bar, Merch Table, Gate C) with a draggable meetup pin that snaps on pointerup to the Euclidean-nearest of 5 landmark centers, and ONE meetupStore write re-derives five surfaces in the same render: pin teleports, MeetBanner rewrites ('Meet at Merch Table after song 12', appending '— heading there now' once nowIndex passes), all four FriendBeaconRow walk times recompute from beacon-to-pin geometry (max(1, round(euclid/40)) min; ≤20 px reads 'at the spot' — merch: Maya 4 min/Diego 6/Priya at the spot/Sam 6; bar: 4/spot/6/2), the rail flag holds its row, and the single polite toast announces the move. Enter on the focused pin, MeetBanner 'Change', and an 'All landmarks →' row all open the two-detent (55% / calc(100%−56px)) landmark sheet — 5 'Pin here' rows plus an after-song spinbutton stepper (min = nowIndex+1) — the full non-gesture parity path. CREW: four 72px media rows (id-derived gradient avatars, walk-time statuses) with 44×44 ellipsis + long-press opening the same Nudge/Remove menu; removal is undoOverConfirm (execute + Undo restoring the exact list position; badge derives from CONFIRMED count so removing unconfirmed Sam keeps '3' while the caption flips to '3 of 3 confirmed'). NIGHT: fixed-string schedule rows and removable notes with a true-empty state. At desktop container widths (>560px measured) it renders as a centered 430px phone column, never a stretched relayout. Choose over mobile-transit-companion when the night revolves around a LIVE event timeline (predicted-vs-actual setlist drift) plus spatial meetup coordination on a venue floorplan, not vehicle departures and a trip stepper; choose over mobile-event-rsvp-chat when the event is already ON and the job is logistics (walk-time geometry, meetup pin, set progress), not invitations and conversation; choose over mobile-parking-session when the hero is a multi-tab companion with a draggable map pin and derived crew states, not a single radial meter with tiered pricing; choose over mobile-store-aisle-finder when the map's pin WRITES shared state that other tabs re-derive, not just wayfinding to a static location.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
