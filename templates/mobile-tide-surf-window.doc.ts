// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Ebbline — Tide Window',
  description:
    "MOBILE (390px shell) surf-session planner for Ebbline at Cold Point, frozen Sat Jul 4: a 52px navBar (tide-sine-through-circle brand mark, 'Cold Point' title, 36px WindowScoreRing — r=16, C=100.53, dashoffset C·(1−score/100), live 13px/700 digits) over a 44px preset-chip rail (Dawn patrol / Midday / Golden hour, x-snap scroll at 320), a 44px stepper row (two 96×32 ±30 min steppers with spinbutton values), and the 264px tideCard — a measured-width SVG tide curve (17 hourly heights 05:00–21:00, pxPerHour = plotWidth/16 → 20.875 at 390) carrying the SIGNATURE two-thumb bracket: 20px brand-ring knobs in 44×44 role=slider hits riding ON the curve, 2px stems to the baseline, and a shaded under-curve region whose opacity encodes the score band (≥80 → 26%, 50–79 → 18%, <50 → 10%). Dragging a handle snaps to half-hour ticks (clamps 05:30/20:30 with 8px rubber-band overshoot and a daylight-cap darkness-refusal pulse, span 1–6 h) and ONE sessionWindow re-derives the 0–100 score in render — Dawn 6–9 = (88+92+96)/3 = 92 exactly, Midday 58, Golden 66, half-tick 06:30–09:00 → 93 — through a 16-slot table whose tide/wind/light triples each sum to their slot total. Three 28px condition lanes share the curve's x mapping (hourly SWELL bars, the WIND band flipping offshore→onshore at a labeled 11:00 marker, a DAYLIGHT gradient with hard night caps at 05:24/20:48) under a full-height window highlight column. The 8-spot ranked list argues back: 72px SpotRankRows (32px rank block with movement arrows vs the frozen Dawn baseline, ±delta chips — ferryflats at dawn shows −41; 'Show 3 more' → focus row 6 → 'All 8 spots'), reordering via 200ms translateY on absolutely-positioned rows; the identical derivation feeds the Spots tab (52px searchBar with recents/results/filtered-empty states) so both tabs always agree. The footer 'Save window · 92' appends to savedSessions (Windows tab badge 1→2, exact-snapshot Undo through the sticky toast dock), then flips to 'Invite crew' opening a 55%-detent sheet whose read-only prefill quotes the single derived string 'Cold Point · Sat 6:00–9:00 AM · Window score 92'; sending to n selected crew is execute-with-Undo, and 'Delete saved window' lives behind a row ellipsis menu (undoOverConfirm, never a confirm dialog). At desktop container widths (≥720px) it renders as a centered 430px phone column, never a stretched relayout. Choose over mobile-parking-session when the object is a CONDITIONS window scrubbed along a natural curve with a ranked list arguing back, not a running paid session with tiered pricing and extend/end verbs; choose over mobile-tip-pool-split when the drag redistributes a score across aligned time lanes rather than money across people; choose over mobile-route-manifest when the timeline is continuous environmental data with a two-handle bracket, not a discrete stop queue; choose over mobile-allergy-season-radar or any read-mostly conditions surface when the brief demands a draggable two-thumb range whose every half-hour tick recomputes scores, ranks, and an invite prefill from one derivation function.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
