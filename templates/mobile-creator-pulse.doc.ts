// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Glowline — Creator Pulse',
  description:
    "MOBILE (390px shell) creator-analytics vitals surface for Glowline, frozen at 11 PM, where TIME is the primary control: a 52px navBar (28px heartbeat-arrow mark, compact 'Pulse' title that fades in via IntersectionObserver when the 52px large-title row scrolls under, an 11px playhead-context subtitle — '11:00 PM — now' → '7:14 PM — reel posted' at h19 — and a 44×44 RefreshCw) over a 244px PulseScrubCard: a 120px measured-width area sparkline of 24 hourly view counts (sum exactly 17,980), a draggable playhead (role=slider 0–23, ArrowLeft/Right, Home/End, aria-valuetext '7:00 PM') with a ±8px ABSOLUTE spike magnet onto three 10px dot buttons at 7 AM / 4 PM / 7 PM (adjacent h16/h19 never double-capture at 320px), ±1h chips and a 'Jump to spike' cycle button as the no-gesture path. Scrubbing retimes EVERYTHING from the one playheadHour atom: three 72px DeltaStack rows whose 24px SplitPills show cumulative-at-playhead | now (Followers 48,175 | 48,264 at 7 PM; 48,264 | 48,264 at 11 PM renders with no fake delta arrow), the top-post thumbnail+title swapping by hour bucket (h0–8 / h9–18 / h19–23 over three id-derived SVG gradient thumbs), the highlighted post row (8% wash + 3px inset bar), and the navBar subtitle — and landing on 7 PM appends to seenSpikes, clears BOTH linked alerts, drops the tab badge 2→0, and announces 'Alerts reviewed'. Tapping a DeltaStack row opens the two-detent (55% / calc(100%−56px)) metric sheet — 24 × 44px hour rows (h3 renders a true-minus '−1' in text-secondary, never error red), playhead row tinted 12% at weight 600, each row a button that moves the playhead — with scroll-locked shell, focus({preventScroll:true}), and opener-restored focus on X/scrim/Escape. RefreshCw swaps the DeltaStack for three deterministic 72px skeleton rows (60/45/70 + 40/55/30 bars, shimmer removed under reduced motion) that resolve on the NEXT user tap — no timers. The 4-tab shell (Pulse / Posts / Audience / Alerts) keeps per-tab scroll, closes overlays but not the toast on switch, scroll-to-tops on active-tab re-tap, and the Alerts tab's 60px rows swipe-to-reveal a 72px 'Mark reviewed' block (mandatory 44×44 ellipsis fallback menu) that executes immediately with an exact-prior-state Undo toast (undoing the second review restores a2 AND badge 1). Fixture laws reconcile exactly: post views 4,120+6,480+7,380 = 17,980 = the hourly sum; follower deltas sum +264 (48,000 → 48,264); audience buckets +2+9+30+31+103+89 = 264. At desktop container widths (≥720px) it renders as a centered 430px phone column. Choose over mobile-parking-session when the hero control is a scrubable TIME axis that retimes a metrics stack, not a live radial meter with a paid-time extend sheet; choose over mobile-tide-surf-window when the scrubber is a single playhead over discrete hourly counts with spike magnets, not a two-thumb window bracket scoring a curve; choose over mobile-stargazing-tonight when the domain is creator analytics (followers/views/posts with SplitPill at-vs-now comparisons and alert triage), not sky-object visibility projection; choose over mobile-banking-home-insights when the screen is a single-day pulse instrument with a draggable playhead, not a balances-and-insights feed.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
