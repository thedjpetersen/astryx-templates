// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Sotto — Topic Mute Manager',
  description:
    "MOBILE (390px shell) mute-rule control panel for the Sotto reader that shows what silence costs: a 52px navBar (Sotto speech-bubble mark whose interior waveform flattens to a line at the tail, 'Sotto' h1, 44×44 RefreshCw → static 'Updated just now'), a 52px searchBar (36px field, focus-flip Cancel, recents 'nba'/'strikes'/'ai' with per-row removes, per-keystroke results with 600-weight substring matches, verbatim-echo no-results state), an 84px DensityMeter card whose 22/700 tabular numeral and 8px two-segment bar derive live from the rules (BASE_DAILY 49: Layoffs hides 6/day, Crypto 5/day → '38 stories / day', 11 hidden, 38 + 11 = 49 ✓, 4px min-segment guard for the 21-visible all-muted edge), ACTIVE MUTES · 2 rows with draining ExpiryCountdownChips (64×24 pill in a 44px hit — Layoffs 96/168 h = 57% fill '4d left', Crypto 6/24 = 25% '6h left'; label sits over the track so 1%-fill stays readable; tap = extend) and ellipsis menus (Extend… / Edit duration… / destructive Unmute now → 49 − 5 = 44 visible ✓), SUGGESTED rows (NBA Playoffs 14/day, Election Polls 3/day) with 88×36 Mute pills, and a full-bleed FeedImpactPreview of 20 sample rows (14 NBA + 6 other, 72px id-derived-gradient thumbs) interleaved with 44px ghost stubs whose tap-to-peek shows 3 dimmed headlines + 'and N more' (14 = 3 + 11 ✓; Election Polls 3 + 0 → no caption). The signature move is undo-over-confirm muting: tapping Mute on NBA commits immediately — its 14 rows accordion-collapse (height/opacity 240ms, opacity swap under reduced motion) into one '14 hidden — NBA Playoffs' stub, the meter re-flexes 38 → 24, ACTIVE MUTES gains a 100%-fill '24h left' row, the Shelf tab badge jumps 11 → 25 (24 + 25 = 49 ✓), and the persistent sticky-in-flow toast (bottom 76, above the 64px tabBar; NO auto-dismiss timer) offers exact-prior-state Undo that returns rows in original order and then reads 'Restored'. Chip taps and 'Mute with duration…' open a two-detent (55% / calc(100% − 56px)) MuteDurationWheel sheet — a scroll-snap vertical wheel of five 44px radio stops ('Until tonight — 9:00 PM', 24 hours, 7 days, 30 days, 'Until this story ends — NBA Finals conclude Jun 22') behind a fixed selection lens with ArrowUp/Down as the non-gesture path and a footer button that rebinds to 'Mute for 24 hours' / 'Mute until tonight'; the shell scroll-locks while open and the toast dock flips shell-absolute. Feed/Mutes/Shelf tabs persist scrollTop + query per tab id; active-tab re-tap scrolls to top; zero required gestures anywhere. At desktop container widths (≥720px) it renders as a centered 430px phone column with hairline borders, never a stretched relayout. Choose over mobile-subscription-audit when the mutation is suppressing a CONTENT TOPIC whose cost shows as feed density and ghost-collapsed rows, not triaging paid subscriptions with money odometers; choose over mobile-parking-session when the sheet picks a categorical duration stop on a snap wheel rather than stepping continuous minutes against a price curve; choose over mobile-transit-companion when the screen is a rules-ledger with live preview consequences, not a live journey tracker; choose over any desktop template when the brief needs the 390px shell with sticky navBar/tabBar, bottom sheet, ghost stubs, and 44×44 touch targets.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
