// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Twofold — Caption A/B Lab',
  description:
    "MOBILE (390px shell) creator workbench for A/B-testing post captions, for the fictional Twofold studio (twofold.studio): a 52px navBar (24px split-T Twofold mark with a 4px amber notch tick, draft-title h1 ellipsized at 200px, and a derived 'A primary' amber chip — #92400E on #FEF3C7 = 6.9:1) over a STICKY previewDock whose 236px VariantCrossfadePreview (48px author row + 96px 4-line caption stage with a 24px fade mask + 44px crossfade rail + 36px WordDiffRail + 12px padding) physically morphs the caption between Variant A and Variant B: shared words render once at fixed positions while A-only spans fade/rise (opacity 1−blend, −6px·blend) and B-only spans settle in (opacity blend, +6px·(1−blend)) — the merged token stream is its own sizer so the stage never reflows, and the whole morph collapses to an opacity swap under reduced motion. The 44px rail is a role=slider (ArrowLeft/Right step 10, Home/End commit at the poles, aria-valuetext '62% toward Variant B') with 44×44 A/B endcap buttons that nudge blend ±0.25 as the mandatory non-gesture path; releasing a drag past 0.6 commits the other variant as primary, under 0.4 commits back, and the 0.4–0.6 dead zone springs to the current primary's pole with no commit and no toast. Below: twin 112px HookScoreGauge semicircles on one honest 0–100 axis (pure per-keystroke formula: 40/24 len + 28/12 '?' + 16/8/4 emoji; fixture d1 scores A 40+28+8 = 76 vs B 40+12+16 = 68, delta '+8 A') whose arc + numeral opacity tilt 0.45→1 against each other as the handle drags; a zone-tagged WordDiffRail whose amber/slate tick lanes are genuine LCS outputs (HOOK 2/1 · BODY 3/4 · CTA 1/1 spans = 12 diverging, 19 shared words — the Signals card re-derives the same numbers); two variant cards with a travelling CrownBadge, 36px 'Set as primary' buttons, and 44×44 pencils opening a two-detent (55% / calc(100%−56px)) editor sheet whose textarea re-scores the gauges behind the scrim on every keystroke. Committing a primary fans out through ONE drafts store: navBar chip, crown, Drafts-tab snippet (first 6 words of the new primary), Publish-tab 'ships: Variant B' chip, and a new History event ('All 6 events' → 'All 7 events', recomputed from length), all restored by the single sticky-dock Undo toast. Four tabs (Drafts 72px media rows with fixed-gradient thumbs · Lab · History full-bleed 60px timeline of exactly 6 seeded events · Publish 44px rows + 'Publish 1 ready draft' in-flow CTA) persist scroll per tab; re-tapping the active tab scrolls to top. At desktop container widths (≥720px) it renders as a centered 430px phone column with hairline borders, never a stretched relayout. Choose over mobile-resale-listing-composer when the creator's object is TWO competing texts diffed word-by-word with a draggable blend and score gauges, not one listing's price/photos/condition pipeline; choose over mobile-story-planner when the work is comparative copywriting (diff spans, hook scores, primary flags) rather than ordering frames on a time budget; choose over mobile-speaking-drill-coach when the diff is between two caption variants the user edits live, not between an attempt and a native-speaker reference; choose over any desktop template when the brief calls for a phone-first 390px shell with sticky navBar/tabBar, a bottom editor sheet, drag-with-button-path gestures, and 44×44 touch targets.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
