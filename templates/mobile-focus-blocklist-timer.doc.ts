// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Bulwark — Focus Blocklist Timer',
  description:
    "MOBILE (390px shell) single-purpose app-blocking focus timer for Bulwark — an immersive navBar / sticky-timerStage / scrolling-blocklist / sticky-Arm-footer stack with NO tabBar. The 344px timerStage (identical height idle and armed — zero shift) holds a 240px CommitmentDial: a 270° rotary (12px stroke, ≥3:1 rest track, 5m snaps over 10–120 min) whose 28px knob is a real role='slider' button (ArrowUp/Down ±5m, PageUp/PageDown jump the 25/50/90 detents, pointer drag maps atan2→value) coupled to escalating strictness tiers — Gentle <50m, Firm 50–85m, Locked ≥90m — so crossing 50 or 90 fires a ratchet: the tier badge pulses (transform-only), the COMMITMENT contract card swaps its typed-phrase terms, and the BlocklistArmingTray pre-dims newly force-contained apps at 60% with a 14px lock. The tray is one listCard split by a 48px shieldEdge band (contrast-checked gradient) into 'Contained · 6' / 'Exempt · 2' zones over 8 fixture apps whose daily averages sum to exactly 90 min (Chirpr 12 … Streamly 21 · MailPile 4); whole 72px rows are role='switch' toggles — Firm force-contains Streamly (avg 21 ≥ 10 → 7+1), Locked contains all 8 with a 'Locked tier — no exemptions' caption. Arming slides contained rows 8px beneath the shield, swaps switches for lock glyphs, flips the navBar title to an '08:00 / 50:00' tabular clock, and turns the dial into the BreachLedger: a full-360° elapsed sweep where four scripted breaches (ReelRoll 8m, Chirpr 19m, Streamly 27m, ReelRoll 41m → 69 min saved, ticks at 57.6°/136.8°/194.4°/295.2°) land as 4×16 error ticks, each a 44×44 button that writes 'ReelRoll at 41m — 18 min saved' into the stage caption. The deterministic clock is the footer's 'Skip 5m' scrub — exactly 10 presses complete the 50m default, banking the PROOF stats card from 6-day streak / 290 week-min / 19 survived to 7/340/23. Early exit is the product's one sanctioned confirm: a two-detent sheet (55% / calc(100%−56px)) demanding the tier's typed phrase ('i am choosing distraction'), validating on blur with an inline fieldError, then a recap sheet with a 96px ring thumbnail whose breach ledger rows appear only at the large detent; the idle HistoryIcon replays yesterday's 90m Locked recap (LootBoxer at 62m, +14). Sticky-in-flow toastDock (no timers), focus({preventScroll:true}) sheet entry, Escape-topmost layering, desktop container widths (≥720px) render a centered 430px phone column. Choose over mobile-parking-session when the dial COMMITS a duration-to-strictness contract with ratcheting tiers and a typed-phrase exit rather than metering elapsed paid time with tiered pricing; choose over mobile-connection-dash when the scripted timeline is scrubbed by the user against a blocklist (breach ticks on a ring) instead of checkpoint taps against a transfer buffer; choose over mobile-banking-home-insights when the single state owner drives an arm/disarm session machine, not a filter-the-ledger insight surface; choose over any desktop template when the brief needs a phone-first 390px shell with sticky chrome, bottom sheets, and 44×44 touch targets.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
