// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Firstlight — Morning Briefing',
  description:
    "MOBILE (390px shell) time-budgeted news digest for Firstlight, dated Friday, July 4: a 52px navBar (28px half-risen-sun mark, fade-in compact title via IntersectionObserver sentinel, 44×44 Timer + Refresh) over a 52px 'Morning Briefing' large title, a 'Friday, July 4 · 11 stories' dateline with a fixed 'Updated just now' role=status caption, a 64px BudgetSummaryStrip ('15-min briefing · 9 of 11 stories · done by 8:14'), and four fixed-order sections (World / Local / Markets / One Good Read) of DepthToggleStoryRows at EXACT zero-shift heights — headline 56px ('Scan' chip, cost 0), summary 116px (2-line dek, '2 min'), full 172px ('FULL READ · 6 MIN' overline + excerpt) — each row TWO buttons: a check-off toggle named by the title and a depth chip that cycles headline→summary→full as a per-story override. ONE pure greedy selector composes everything: pass 1 upgrades headline→summary in editorial rank order (g1, w1, m1, w2, m2, l1, m3, l3, w3, l2, w4; Σ summaryMin = 18) while cost stays ≤ budget, pass 2 upgrades summary→full — pre-verified: budget 15 → cost 14 / 9 of 11 / done by 8:14 (breakdown 6+3+3+2), budget 8 → 8 / 5 of 11, budget 10 → 10 / 6 of 11, budget 5 → 5 / 3 of 11, budget 20 → 20 / 11 of 11 with l1 landing FULL. The two-detent (55% / 100%−56px) budget sheet holds the 240×140 semicircular TimeBudgetDial (role=slider 5–20 min, 16 tick 'sun rays' 12° apart, brand budget arc r96/12 over a ≥3:1 rest track, inner cost arc r78/6 that flips amber and sweeps past the budget arc when overrides exceed it), four 44px preset chips (exact match filled, nearest ±2 outlined), a Composition breakdown whose section minutes sum exactly to the composed cost, and — at the large detent — the 11-row priority list with the same depth chips. The 48px BudgetBurndownBar (sticky bottom:64 above the 64px tabBar, 4px fill track) reads '14 min left · done by 8:14 · 0 of 11 read', flips to '+N min over' in the override-over-budget state, and burns down as check-offs fire persistent execute-with-Undo toasts in the sticky-in-flow toast dock (bottom:112); the Today tab badge counts unread 11→0 and is removed at zero. Explore is a true-empty state, Saved holds three 60px rows (w3, g1, l2) with bookmark-removal Undo, Me has a whole-row role=switch reminder plus a Time-budget row reading the live budget; per-tab scroll persists and re-tapping Today pops to top. At desktop container widths (≥720px) it renders as a centered 430px phone column. Choose over mobile-parking-session when the dial rations a READING budget that recomposes list depth in place (content rationing), not a metered session with pricing tiers and extend/end verbs; choose over mobile-podcast-queue or other media-list surfaces when rows morph between three fixed depth states under one greedy budget algorithm rather than reordering a play queue; choose over mobile-subscription-audit when the aggregate being conserved is minutes-to-done projected as a finish time, not dollars reclaimed by verdict triage; choose over any desktop news/dashboard template when the brief calls for a phone-first 390px shell with sticky bottom chrome, a bottom-sheet control surface, and 44×44 touch targets.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
