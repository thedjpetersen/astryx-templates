// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Shuttlebee — Carpool Week Coordinator',
  description:
    "MOBILE (390px shell) carpool fairness machine for the Shuttlebee school-run pod of 4 families, 'Week of Mar 2–6': a 52px navBar (28px bee route-loop mark, per-tab title, 44×44 RefreshCw) over a 56px sticky Mon–Fri weekStrip (5 flex-1 dayCells, 71.6px at 390 / 57.6px at 320, each a 13px/600 day label + tabular date under a shape-coded status dot — solid disc covered, hollow red ring open, amber triangle backup — never color alone), then per-day AM/PM run cards: 11px/500 'AM RUN · DEPART 7:35' overline + 22px status chip, a 72×72 DriverSlotCoin (56px face + 4px fairness arc, r=30 / C=188.5: settled given/owed sweep — Bennett 6/8 = 270° — lighter-honey pending ticks derived live from this week's claims, and Nakamura's hatched amber provisional +1 while Thu PM stands open) beside a 60px driver stack, a 72px RouteRibbon SVG of id-derived gradient-roof house stops with strictly increasing +min offsets (pure function of driverId; open runs render the path dashed with 'Stop order set by driver'), and a 96px SeatPillRow minivan cross-section (row 2 = 2 booster-capable pills with Armchair glyphs, row 3 = 3; seatKids sorts boosters first and throws in dev past row-2 capacity; Fri AM rides full at 5/5). Tabs: Week / My runs (72px rows + mini ribbon strips, 'All 2 runs' → 'All 3 runs') / Families (40px mini fairness coins, '6 given · 8 owed', net chips −2 behind / +2 ahead / even, footer 'All 4 families · 28 drives settled · 28 owed') / Alerts (All|Unresolved segmented, 60px rows with 44×44 dismiss + Undo, BellOff filtered-empty with 'Show all'). The signature move is the claim transaction: Thursday PM's open coin (coin tap or the 48px 'Claim this run' button — both visible paths) opens a two-detent (55% / 100%−56px) sheet whose route preview, 'Milo joins · 4 of 5 seats' seat impact, and 'Your balance −2 → −1' fairness delta all derive from the same pure functions, and ONE weekStore write flips six surfaces at once — coin → your avatar, every fairness arc rebalances (your pending tick appears, Nakamura's hatched provisional relaxes away), boosters Milo+Lucía force into row 2 of your Odyssey, the ribbon reorders to School +0 → Osei +4 → Nakamura +9 → Delgado +13 → Bennett +17, Thu's red strip dot turns solid (coverage 9 of 10 → 10 of 10), and the Alerts badge drops 3→2 as 'Thursday PM needs a driver' self-resolves — with exact-snapshot Undo through the sticky toast dock (undo-over-confirm, no dialogs anywhere); 'Request a backup instead' and Fri PM's needs-backup coin (amber ring pulse, static under reduced motion, LifeBuoy badge) reuse the same sheet in backup mode. RefreshCw press 1 shows two deterministic skeleton run cards (60/45/70 + 40/55/30 bar widths) with 'Loading' announced once, press 2 resolves to a fixed 'Updated just now' caption. At desktop container widths (≥720px) it renders as a centered 430px phone column with hairline borders, never a stretched relayout. Choose over mobile-parking-session when the object is a SHARED weekly roster whose fairness ledger rebalances across four parties on one claim, not a single metered session with tiered pricing; choose over mobile-transit-companion when the surface coordinates people covering slots (driver coins, seat plans, fairness arcs) rather than tracking a vehicle already moving; choose over mobile-plant-care-roster when the schedule is negotiated between peer families with a debt ledger and backup obligations, not a solo care checklist; choose over any desktop template when the brief calls for a phone-first 390px shell with sticky week strip, bottom tab bar, two-detent sheets, and 44×44 touch targets.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
