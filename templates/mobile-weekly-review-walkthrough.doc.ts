// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Sevenfold — Weekly Review Walkthrough',
  description:
    "MOBILE (390px shell) GTD weekly review as a seven-step, thumb-paced walkthrough for Sevenfold (olive #4D7C0F brand): a 52px navBar carrying a 32px seven-notch StepDial (ONE merged 44px-tall role='slider' control with a tabular '3/7' fraction — arrow keys step, pointerdown on a notch jumps, the 7th notch is a checkmark that inks only on Finish) between a 44×44 prev chevron and a 44×44 ledger button; below it a horizontally paged step view (7 × width:100% pages, translateX(-step*100%), 240ms, instant under reduced motion) walks Sweep inbox (5) → Past-week calendar (3, 48px TUE/09:30 day blocks, dividers inset 68) → Stale tasks (6, tabular 'Captured 47 days ago' ages) → Projects (4, stale badges DERIVED not stored) → Someday (4) → Calendar ahead (3) → Recap, 25 items total (5+3+6+4+4+3 = 25 cross-checked). Every page header is 109px (11px overline + 22px h2 + live 24px SweepCounter pill whose number flips 12px on decrement and swaps to a brand 'Clear' pill at 0); every item is a 116px DispositionRow with an inline Do/Defer/Delegate/Drop verb strip (4 × flex:1 44px segments, 6px gaps) that STAGES verdicts into a session ledger — stamped rows collapse to a 3px verdict edge bar + 24px chip inside a 44px reopen target. The signature move is the cascade: dropping project p-02 'Garage rebuild' on step 4 auto-stamps its 3 stale tasks on step 3 as Drop with 'with Garage rebuild' provenance, decrements step 3's counter 6→3 visibly, and raises the sticky Undo toast ('Garage rebuild dropped — 3 stale tasks cleared') whose Undo deletes ONLY cascade:p-02 entries so direct verdicts survive; stamping the last unresolved item on a page auto-advances the pager after 400ms and inks the matching notch. Step 7 is a ReviewRecapCard derived entirely from the ledger at render: 56px mini dial, 28px tabular '25 of 25 items cleared' headline, 2×2 verb stat grid (Drop cell grows 'incl. 3 via dropped project'), oldest-resolved (47 days or an em dash) and carried-forward lines. No tabBar; a sticky-footer 48px 'Next step' button always advances ('Finish review' → disabled 'Review saved' on step 7); a 55% half-detent DispositionSheet (Zap/Clock/UserRound/Trash2 56px verb rows, Drop last in error, 'Clear verdict' when stamped) is every gesture's button path, and the navBar LedgerSheet groups staged verdicts by verb with counts summing to verdicts.size. Toasts dock sticky-in-flow at bottom 92 (absolute while sheet-locked); no timers, one at a time. At desktop container widths (≥720px) it renders as a centered 430px phone column with hairline borders, never a stretched relayout. Choose over mobile-dm-request-triage when the review is a MULTI-STEP paged walkthrough with a session ledger and cross-step cascade consequences, not a single-list accept/decline queue; choose over mobile-parking-session when the sheet stages verdicts across seven pages rather than driving one live object's dial/price/receipts; choose over mobile-story-planner when the one-store consequence is verdict cascade and counter decrement across steps, not drag-reorder rippling through duration math; choose over any desktop template when the brief calls for a phone-first 390px shell with a paged stepper, bottom sheets, sticky footer, and 44×44 touch targets.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
