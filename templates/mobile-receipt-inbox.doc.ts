// Copyright (c) Meta Platforms, Inc. and affiliates.

import type {AstryxPageTemplate} from '@astryxdesign/cli/template';

const template = {
  type: 'page',
  name: 'Shoebill — Receipt Inbox',
  description:
    "MOBILE (390px shell) receipts inbox for Shoebill with a tactile thermal-paper loop, frozen against the June statement total $2,184.62: a 52px navBar (24px shoebill-head mark whose beak is a perforated receipt stub, per-tab h1, 44×44 Refresh) over a 52px filter-chip rail whose counts derive live ('All 4' / 'Low confidence 3' / 'This week 4'), then a 452px inboxStack — the top receipt is a deterministic ThermalReceipt SVG (viewBox 358×420: 17-unit mono merchant header, four line items + SUBTOTAL/TAX/TOTAL that reconcile to the cent — Trader Joe's 12.99+8.49+22.47+16.98 = 60.93 + 6.20 tax = 67.13 — dashed rules, a dashed perforation, and an 18-unit triangle-wave tear-off stub) over two ghost cards reading '3 more behind'. Four 36px FieldConfirmChips (44px hits) bind to highlight rects on the stub: low-confidence chips pulse (static warning fill under reduced motion), tapping one opens the two-detent confirm sheet (55% / calc(100%−56px)) focused on that field with preventScroll and flashes its receipt region 3×300ms; the sheet holds a 48px merchant input, a decimal amount input (blur validation), an inline 2026 datePanel grid with arrow-key days, a category chip grid, and a sticky 'File receipt · $67.13' footer that is never silently disabled — unconfirmed presses show '2 fields unconfirmed' in error red, shake the offending chip, and focus it. The signature move: swipe the receipt up past −110px (button paths: sheet footer + the card's 44×44 ellipsis menu, destructive 'Discard receipt' last) and ONE dispatch stamps FILED, tears the perforated stub 12px, promotes the next ghost, drops the Inbox badge 4→3, prepends the filed row ('FILED THIS MONTH · 6'→7), ticks the 96px ReconciliationBar 46.6%→49.6% ($1,017.24→$1,084.37 matched of $2,184.62), mints the Reports 'Groceries' row, and raises a no-timer Undo toast whose Undo restores every number from one snapshot. Filing all four reaches 'Inbox zero' at $1,307.28 matched / 59.8%. Filed/Reports/Settings tabs persist scroll + filter state (re-tap active tab scrolls to top); Refresh shows three deterministic 60px skeleton rows (widths 60/45/70 + 40/55/30) resolved by the next tap into 'Updated just now'. At desktop container widths (≥720px) it renders as a centered 430px phone column with hairline borders, never a stretched relayout. Choose over mobile-parking-session when the object is a triage QUEUE of documents with per-field confidence confirmation and a running reconciliation ledger, not one live metered session with a countdown gauge; choose over mobile-banking-home-insights when the loop is extract-confirm-file against a statement rather than browsing balances and spend insights; choose over mobile-voice-memo-triage when the artifacts are visual paper stubs whose extracted fields flash bound highlight regions, not audio transcripts minting chips; choose over any desktop template when the brief calls for a phone-first 390px shell with sticky navBar/tabBar, a bottom sheet, swipe gestures with button fallbacks, and 44×44 touch targets.",
  category: 'Mobile',
  componentsUsed: ['Icon'],
} satisfies AstryxPageTemplate;

export default template;
